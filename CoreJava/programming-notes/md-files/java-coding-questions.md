<a id="top"></a>

# Java Coding Questions

## Table of Contents

| # | Topic | Level |
|---|-------|-------|
| 1 | [Reverse a String ](#Reverse-a-String) | 🟢 Basic |
| 2 | [Check Palindrome ](#Check-Palindrome) | 🟢 Basic |
| 3 | [Factorial of a Number ](#Factorial-of-a-Number) | 🟢 Basic |
| 4 | [Fibonacci Series  ](#Fibonacci-Series ) | 🟢 Basic |
| 5 | [Prime Number Check  ](#Prime-Number-Check ) | 🟢 Basic |
| 6 | [Swap Two Numbers  ](#Swap-Two-Numbers ) | 🟢 Basic |
| 7 | [Find Largest Element in Array  ](#Find-Largest-Element-in-Array ) | 🟢 Basic |
| 8 | [Sum of Array Elements  ](#Sum-of-Array-Elements ) | 🟢 Basic |
| 9 | [Count Vowels in String  ](#Count-Vowels-in-String ) | 🟢 Basic |
| 10 | [Check Armstrong Number  ](#Check-Armstrong-Number ) | 🟢 Basic |
| 11 | [Count Digits in Number  ](#Count-Digits-in-Number ) | 🟢 Basic |
| 12 | [Reverse Number  ](#Reverse-Number ) | 🟢 Basic |
| 13 | [Check Even or Odd  ](#Check-Even-or-Odd ) | 🟢 Basic |
| 14 | [Sum of Digits  ](#Sum-of-Digits ) | 🟢 Basic |
| 15 | [GCD of Two Numbers  ](#GCD-of-Two-Numbers ) | 🟢 Basic |
| 16 | [LCM of Two Numbers  ](#LCM-of-Two-Numbers ) | 🟢 Basic |
| 17 | [Binary to Decimal  ](#Binary-to-Decimal ) | 🟢 Basic |
| 18 | [Decimal to Binary  ](#Decimal-to-Binary ) | 🟢 Basic |
| 19 | [Linear Search  ](#Linear-Search ) | 🟢 Basic |
| 20 | [Bubble Sort  ](#Bubble-Sort ) | 🟢 Basic |
| 21 | [Remove Duplicates from Array](#Remove-Duplicates-from-Array ) | 🟢 Basic |
| 22 | [Remove Duplicates from Array](#Remove-Duplicates-from-Array ) | 🟢 Basic |
| 23 | [Count Words in String](#Count-Words-in-String ) | 🟢 Basic |
| 24 | [Find Second Largest Number](#Find-Second-Largest-Number ) | 🟢 Basic |
| 25 | [Check Anagram](#Check-Anagram ) | 🟢 Basic |
| 26 | [Matrix Addition](#Matrix-Addition ) | 🟢 Basic |
| 27 | [Check Leap Year](#Check-Leap-Year ) | 🟢 Basic |
| 28 | [Print Multiplication Table](#Print-Multiplication-Table ) | 🟢 Basic |
| 29 | [Find Factor of Number](#Find-Factor-of-Number ) | 🟢 Basic |
| 30 | [Sum of First N Natural Numbers](#Sum-of-First-N-Natural-Numbers ) | 🟢 Basic |
| 31 | [Check Perfect Number](#Check-Perfect-Number ) | 🟢 Basic |
| 32 | [Count Frequency of Elements](#Count-Frequency-of-Elements ) | 🟢 Basic |
| 33 | [Find Missing Number in Array](#Find-Missing-Number-in-Array ) | 🟢 Basic |
| 34 | [Check Sorted Array](#Check-Sorted-Array ) | 🟢 Basic |
| 35 | [Rotate Array by One](#Rotate-Array-by-One ) | 🟢 Basic |
| 36 | [Find Duplicate Element](#Find-Duplicate-Element ) | 🟢 Basic |
| 37 | [Reverse Array](#Reverse-Array ) | 🟢 Basic |
| 38 | [Merge Two Arrays](#Merge-Two-Arrays ) | 🟢 Basic |
| 39 | [Merge Two Arrays](#Merge-Two-Arrays ) | 🟢 Basic |
| 40 | [Find Common Elements](#Find-Common-Elements ) | 🟢 Basic |
| 41 | [Remove Spaces from String](#Remove-Spaces-from-String ) | 🟢 Basic |
| 42 | [Convert String to Uppercase](#Convert-String-to-Uppercase ) | 🟢 Basic |
| 43 | [Count Characters in String](#Count-Characters-in-String ) | 🟢 Basic |
| 44 | [Find First Non-Repeating Character](#Find-First-Non-Repeating-Character ) | 🟢 Basic |
| 45 | [Check String Contains Only Digits](#Check-String-Contains-Only-Digits ) | 🟢 Basic |
| 46 | [Convert String to Integer](#Convert-String-to-Integer ) | 🟢 Basic |
| 47 | [Find Largest Word in Sentence](#Find-Largest-Word-in-Sentence ) | 🟢 Basic |
| 48 | [Check Two Strings Equal](#Check-Two-Strings-Equal ) | 🟢 Basic |
| 49 | [Toggle Case of Characters](#Toggle-Case-of-Characters ) | 🟢 Basic |
| 50 | [Count Occurrence of Character](#Count-Occurrence-of-Character ) | 🟢 Basic |
| 51 | [Remove Character from String](#Remove-Character-from-String ) | 🟢 Basic |
| 51 | [Sort Characters in String](#Sort-Characters-in-String ) | 🟢 Basic |
| 52 | [Sum of Even Numbers in Array](#Sum-of-Even-Numbers-in-Array ) | 🟢 Basic |
| 53 | [Sum of Odd Numbers in Array](#Sum-of-Odd-Numbers-in-Array ) | 🟢 Basic |
| 54 | [Find Smallest Element](#Find-Smallest-Element ) | 🟢 Basic |
| 54 | [Copy Array](#Copy-Array ) | 🟢 Basic |
| 55 | [Check Array Equality](#Check-Array-Equality ) | 🟢 Basic |
| 56 | [Find Index of Element](#Find-Index-of-Element ) | 🟢 Basic |
| 57 | [Count Positive and Negative Numbers](#Count-Positive-and-Negative-Numbers ) | 🟢 Basic |
| 58 | [Move Zeros to End](#Move-Zeros-to-End ) | 🟢 Basic |
| 59 | [Find Intersection of Arrays](#Find-Intersection-of-Arrays ) | 🟢 Basic |
| 60 | [Find-Union-of-Arrays](#Find-Union-of-Arrays ) | 🟢 Basic |
| 61 | [Reverse String Using Recursion](#Reverse-String-Using-Recursion ) | 🟢 Basic |
| 62 | [Factorial Using Recursion](#Factorial-Using-Recursion ) | 🟢 Basic |
| 63 | [Fibonacci Using Recursion](#Fibonacci-Using-Recursion ) | 🟢 Basic |
| 64 | [Power of Number (Recursion)](#Power-of-Number-(Recursion) ) | 🟢 Basic |
| 65 | [Sum of Digits (Recursion)](#Sum-of-Digits-(Recursion) ) | 🟢 Basic |
| 66 | [Binary Search](#Binary-Search ) | 🟢 Basic |
| 67 | [Selection Sort](#Selection-Sort ) | 🟢 Basic |
| 68 | [Insertion Sort](#Insertion-Sort ) | 🟢 Basic |
| 69 | [Check Prime (Optimized)](#Check-Prime-(Optimized) ) | 🟢 Basic |
| 70 | [Find All Prime Numbers up to N](#Find-All-Prime-Numbers-up-to-N ) | 🟢 Basic |
| 71 | [Transpose Matrix](#Transpose-Matrix ) | 🟢 Basic |
| 72 | [Diagonal Sum of Matrix](#Diagonal-Sum-of-Matrix ) | 🟢 Basic |
| 73 | [Check-Identity-Matrix](#Check-Identity-Matrix ) | 🟢 Basic |
| 74 | [Check Sparse Matrix](#Check-Sparse-Matrix ) | 🟢 Basic |
| 75 | [Matrix Multiplication](#Matrix-Multiplication ) | 🟢 Basic |
| 76 | [Two Sum Problem](#Two-Sum-Problem ) | 🟢 Basic |
| 77 | [Maximum Subarray Sum (Kadane’s Algorithm)](#Maximum-Subarray-Sum-Kadanes-Algorithm) | 🟢 Basic |
| 78 | [Best Time to Buy and Sell Stock](#Best-Time-to-Buy-and-Sell-Stock) | 🟢 Basic |
| 79 | [Move Negative Numbers to One Side](#Move-Negative-Numbers-to-One-Side) | 🟢 Basic |
| 80 | [Longest Substring Without Repeating Characters](#Longest-Substring-Without-Repeating-Characters) | 🟢 Basic |
| 81 | [Subarray with Given Sum (Positive Integers)](#Subarray-with-Given-Sum-Positive-Integers) | 🟢 Basic |
| 82 | [Count Subarrays with Given Sum](#Count-Subarrays-with-Given-Sum) | 🟢 Basic |
| 83 | [Majority Element (> n/2)](#Majority-Element-Greater-than-n2) | 🟢 Basic |
| 84 | [Missing Number (0 to n)](#Missing-Number-0-to-n) | 🟢 Basic |
| 85 | [Find Duplicate Number](#Find-Duplicate-Number) | 🟢 Basic |
| 86 | [Product of Array Except Self](#Product-of-Array-Except-Self) | 🟢 Basic |
| 87 | [Maximum Product Subarray](#Maximum-Product-Subarray) | 🟢 Basic |
| 88 | [Rotate Array by K Steps](#Rotate-Array-by-K-Steps) | 🟢 Basic |
| 89 | [Trapping Rain Water](#Trapping-Rain-Water) | 🟢 Basic |
| 90 | [Container With Most Water](#Container-With-Most-Water) | 🟢 Basic |
| 91 | [Merge Intervals](#Merge-Intervals) | 🟢 Basic |
| 92 | [Set Matrix Zeroes](#Set-Matrix-Zeroes) | 🟢 Basic |
| 93 | [Spiral Matrix](#Spiral-Matrix) | 🟢 Basic |
| 94 | [Search in Rotated Sorted Array](#Search-in-Rotated-Sorted-Array) | 🟢 Basic |
| 95 | [Find Peak Element](#Find-Peak-Element) | 🟢 Basic |
| 96 | [Kth Largest Element](#Kth-Largest-Element) | 🟢 Basic |
| 97 | [Top K Frequent Elements](#Top-K-Frequent-Elements) | 🟢 Basic |
| 98 | [Valid Parentheses](#Valid-Parentheses) | 🟢 Basic |
| 99 | [Min Stack](#Min-Stack) | 🟢 Basic |
| 100 | [Evaluate Postfix Expression](#Evaluate-Postfix-Expression) | 🟢 Basic |
| 101 | [Binary Tree Node Creation](#Binary-Tree-Node-Creation) | 🟢 Basic |
| 102 | [Inorder Traversal (Recursive)](#Inorder-Traversal-Recursive) | 🟢 Basic |
| 103 | [Preorder Traversal](#Preorder-Traversal) | 🟢 Basic |
| 104 | [Postorder Traversal](#Postorder-Traversal) | 🟢 Basic |
| 105 | [Level Order Traversal (BFS)](#Level-Order-Traversal-BFS) | 🟢 Basic |
| 106 | [Height of Binary Tree](#Height-of-Binary-Tree) | 🟢 Basic |
| 107 | [Count Nodes in Binary Tree](#Count-Nodes-in-Binary-Tree) | 🟢 Basic |
| 108 | [Sum of Nodes](#Sum-of-Nodes) | 🟢 Basic |
| 109 | [Check Balanced Binary Tree](#Check-Balanced-Binary-Tree) | 🟢 Basic |
| 110 | [Diameter of Binary Tree](#Diameter-of-Binary-Tree) | 🟢 Basic |
| 111 | [Lowest Common Ancestor (Binary Tree)](#Lowest-Common-Ancestor-Binary-Tree) | 🟢 Basic |
| 112 | [Binary Search Tree Insert](#Binary-Search-Tree-Insert) | 🟢 Basic |
| 113 | [Search in BST](#Search-in-BST) | 🟢 Basic |
| 114 | [Delete Node in BST](#Delete-Node-in-BST) | 🟢 Basic |
| 115 | [Graph Representation (Adjacency List)](#Graph-Representation-Adjacency-List) | 🟢 Basic |
| 116 | [DFS (Depth First Search)](#DFS-Depth-First-Search) | 🟢 Basic |
| 117 | [BFS (Graph)](#BFS Graph) | 🟢 Basic |
| 118 | [Detect Cycle in Undirected Graph](#Detect-Cycle-in-Undirected-Graph) | 🟢 Basic |
| 119 | [Number of Connected Components](#Number-of-Connected-Components) | 🟢 Basic |
| 120 | [Flood Fill Algorithm](#Flood-Fill-Algorithm) | 🟢 Basic |
| 121 | [Number of Islands](#Number-of-Islands) | 🟢 Basic |
| 122 | [Topological Sort (DFS)](#Topological-Sort-DFS) | 🟢 Basic |
| 123 | [Dijkstra’s Algorithm](#Dijkstras-Algorithm) | 🟢 Basic |
| 124 | [Bellman-Ford Algorithm](#Bellman-Ford-Algorithm) | 🟢 Basic |
| 125 | [Disjoint Set (Union-Find)](#Disjoint-Set-Union-Find) | 🟢 Basic |
| 126 | [Fibonacci using DP (Memoization)](#Fibonacci-using-DP-Memoization) | 🟢 Basic |
| 128 | [Fibonacci using Tabulation](#Fibonacci-using-Tabulation) | 🟢 Basic |
| 129 | [Climbing Stairs](#Climbing-Stairs) | 🟢 Basic |
| 130 | [House Robber Problem](#House-Robber-Problem) | 🟢 Basic |
| 131 | [Coin Change (Minimum Coins)](#Coin-Change-Minimum-Coins) | 🟢 Basic |
| 132 | [Longest Increasing Subsequence](#Longest-Increasing-Subsequence) | 🟢 Basic |  
| 133 | [Longest Common Subsequence](#Longest-Common-Subsequence) | 🟢 Basic |  
| 134 | [Edit Distance](#Edit-Distance) | 🟢 Basic |  
| 135 | [0/1 Knapsack](#0-1-Knapsack) | 🟢 Basic |  
| 136 | [Subset Sum Problem](#Subset-Sum-Problem) | 🟢 Basic |  
| 137 | [Partition Equal Subset Sum](#Partition-Equal-Subset-Sum) | 🟢 Basic |  
| 138 | [Minimum Path Sum (Grid)](#Minimum-Path-Sum-Grid) | 🟢 Basic |  
| 139 | [Unique Paths](#Unique-Paths) | 🟢 Basic |  
| 140 | [Palindromic Substrings Count](#Palindromic-Substrings-Count) | 🟢 Basic |  
| 141 | [Longest Palindromic Substring](#Longest-Palindromic-Substring) | 🟢 Basic |  
| 142 | [Decode Ways](#Decode-Ways) | 🟢 Basic |  
| 143 | [Word Break](#Word-Break) | 🟢 Basic |  
| 144 | [Longest Common Substring](#Longest-Common-Substring) | 🟢 Basic |  
| 145 | [Rod Cutting Problem](#Rod-Cutting-Problem) | 🟢 Basic |  
| 146 | [Matrix Chain Multiplication](#Matrix-Chain-Multiplication) | 🟢 Basic |  
| 147 | [Egg Dropping Problem](#Egg-Dropping-Problem) | 🟢 Basic |  
| 148 | [Wildcard Matching](#Wildcard-Matching) | 🟢 Basic |  
| 149 | [Burst Balloons](#Burst-Balloons) | 🟢 Basic |  
| 150 | [Palindrome Partitioning](#Palindrome-Partitioning) | 🟢 Basic |  
| 151 | [Boolean Parenthesization](#Boolean-Parenthesization) | 🟢 Basic |  
| 152 | [Sliding Window Maximum](#Sliding-Window-Maximum) | 🟢 Basic |  
| 153 | [Minimum Window Substring](#Minimum-Window-Substring) | 🟢 Basic |  
| 154 | [Largest Rectangle in Histogram](#Largest-Rectangle-in-Histogram) | 🟢 Basic |  
| 155 | [Maximal Rectangle (Binary Matrix)](#Maximal-Rectangle-Binary-Matrix) | 🟢 Basic |  
| 156 | [Median of Two Sorted Arrays](#Median-of-Two-Sorted-Arrays) | 🟢 Basic |  
| 157 | [Merge K Sorted Lists](#Merge-K-Sorted-Lists) | 🟢 Basic |  
| 158 | [LRU Cache](#LRU-Cache) | 🟢 Basic |  
| 159 | [LFU Cache (Basic)](#LFU-Cache-Basic) | 🟢 Basic |  
| 160 | [Serialize and Deserialize Binary Tree](#Serialize-and-Deserialize-Binary-Tree) | 🟢 Basic |  
| 161 | [Trie Implementation](#Trie-Implementation) | 🟢 Basic |  
| 163 | [Word Search (Backtracking)](#Word-Search-Backtracking) | 🟢 Basic |  
| 163 | [N-Queens Problem](#N-Queens-Problem) | 🟢 Basic | 
| 164 | [Sudoku Solver](#Sudoku-Solver) | 🟢 Basic |  
| 165 | [KMP String Matching](#KMP-String-Matching) | 🟢 Basic |  
| 166 | [Rabin-Karp Algorithm](#Rabin-Karp-Algorithm) | 🟢 Basic |  
| 167 | [Z Algorithm](#Z-Algorithm) | 🟢 Basic |  
| 168 | [Quick Sort](#Quick-Sort) | 🟢 Basic |  
| 169 | [Merge Sort](#Merge-Sort) | 🟢 Basic |  
| 170 | [Heap Sort](#Heap-Sort) | 🟢 Basic |  
| 171 | [Counting Sort](#Counting-Sort) | 🟢 Basic |  
| 172 | [Radix Sort](#Radix-Sort) | 🟢 Basic |  
| 173 | [Bucket Sort](#Bucket-Sort) | 🟢 Basic |  
| 174 | [Bit Manipulation (Count Set Bits)](#Bit-Manipulation-Count-Set-Bits) | 🟢 Basic |  
| 175 | [Power of Two Check](#Power-of-Two-Check) | 🟢 Basic |  
| 176 | [Single Number (XOR)](#Single-Number-XOR) | 🟢 Basic |  
| 177 | [Single Number (XOR)](#Single-Number-XOR) | 🟢 Basic |  
---


## Reverse a String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "hello"; 
        String rev = ""; 
         
        for (int i = str.length() - 1; i >= 0; i--) { 
            rev += str.charAt(i); 
        } 
         
        System.out.println(rev); 
    } 
}
```
[⬆ Back to Top](#top)

## Check Palindrome
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "madam"; 
        String rev = new StringBuilder(str).reverse().toString(); 
         
        if (str.equals(rev)) 
            System.out.println("Palindrome"); 
        else 
            System.out.println("Not Palindrome"); 
    } 
} 
```
[⬆ Back to Top](#top)

## Factorial of a Number
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 5; 
        int fact = 1; 
         
        for (int i = 1; i <= n; i++) { 
            fact *= i; 
        } 
         
        System.out.println(fact); 
    } 
}
```
[⬆ Back to Top](#top)

## Fibonacci Series 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 10, a = 0, b = 1; 
         
        for (int i = 0; i < n; i++) { 
            System.out.print(a + " "); 
            int c = a + b; 
            a = b; 
            b = c; 
        } 
    } 
} 
```
[⬆ Back to Top](#top)

## Prime Number Check
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 29; 
        boolean isPrime = true; 
 
        for (int i = 2; i <= n / 2; i++) { 
            if (n % i == 0) { 
                isPrime = false; 
                break; 
            } 
        } 
 
        System.out.println(isPrime ? "Prime" : "Not Prime"); 
    } 
} 
```
[⬆ Back to Top](#top)

## Swap Two Numbers

```java
public class Main { 
    public static void main(String[] args) { 
        int a = 5, b = 10; 
 
        a = a + b; 
        b = a - b; 
        a = a - b; 
 
        System.out.println(a + " " + b); 
    } 
} 
```
[⬆ Back to Top](#top)


## Find Largest Element in Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 5, 3, 9, 2}; 
        int max = arr[0]; 
 
        for (int num : arr) { 
            if (num > max) max = num; 
        } 
 
        System.out.println(max); 
    } 
}
```
[⬆ Back to Top](#top)


## Sum of Array Elements 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4}; 
        int sum = 0; 
 
        for (int num : arr) { 
            sum += num; 
        } 
 
        System.out.println(sum); 
    } 
} 
```
[⬆ Back to Top](#top)


## Count Vowels in String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "hello world"; 
        int count = 0; 
 
        for (char c : str.toCharArray()) { 
            if ("aeiou".indexOf(c) != -1) 
                count++; 
        } 
 
        System.out.println(count); 
    } 
} 
``` 
[⬆ Back to Top](#top)


## Check Armstrong Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 153, sum = 0, temp = n; 
 
        while (temp != 0) { 
            int digit = temp % 10; 
            sum += digit * digit * digit; 
            temp /= 10; 
        } 
 
        System.out.println(sum == n ? "Armstrong" : "Not Armstrong"); 
    } 
} 
```
[⬆ Back to Top](#top)


## Count Digits in Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 12345, count = 0; 
 
        while (n != 0) { 
            count++; 
            n /= 10; 
        } 
 
        System.out.println(count); 
    } 
} 
```
[⬆ Back to Top](#top)

## Reverse Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 123, rev = 0; 
 
        while (n != 0) { 
            rev = rev * 10 + n % 10; 
            n /= 10; 
        } 
 
        System.out.println(rev); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Check Even or Odd 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 7; 
        System.out.println(n % 2 == 0 ? "Even" : "Odd"); 
    } 
}
```
[⬆ Back to Top](#top)

## Sum of Digits 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 123, sum = 0; 
 
        while (n != 0) { 
            sum += n % 10; 
            n /= 10; 
        } 
 
        System.out.println(sum); 
    } 
} 
```
[⬆ Back to Top](#top)

## GCD of Two Numbers 
```java
public class Main { 
    public static void main(String[] args) { 
        int a = 12, b = 18; 
 
        while (b != 0) { 
            int temp = b; 
            b = a % b; 
            a = temp; 
        } 
 
        System.out.println(a); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## LCM of Two Numbers 
```java
public class Main { 
    public static void main(String[] args) { 
        int a = 12, b = 18; 
        int lcm = (a * b) / gcd(a, b); 
        System.out.println(lcm); 
    } 
 
    static int gcd(int a, int b) { 
        while (b != 0) { 
            int temp = b; 
            b = a % b; 
            a = temp; 
        } 
        return a; 
    } 
} 
```
[⬆ Back to Top](#top)

## Binary to Decimal 
```java
public class Main { 
    public static void main(String[] args) { 
        String binary = "1010"; 
        int decimal = Integer.parseInt(binary, 2); 
        System.out.println(decimal); 
    } 
} 
```
[⬆ Back to Top](#top)

## Decimal to Binary 
```java
public class Main { 
    public static void main(String[] args) { 
        int num = 10; 
        System.out.println(Integer.toBinaryString(num)); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Linear Search 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 3, 5, 7}; 
        int key = 5; 
 
        for (int i = 0; i < arr.length; i++) { 
            if (arr[i] == key) { 
                System.out.println("Found at " + i); 
                return; 
            } 
        } 
 
        System.out.println("Not Found"); 
    } 
} 
```
[⬆ Back to Top](#top)

## Bubble Sort 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {5, 2, 8, 1}; 
 
        for (int i = 0; i < arr.length; i++) { 
            for (int j = 0; j < arr.length - i - 1; j++) { 
                if (arr[j] > arr[j + 1]) { 
                    int temp = arr[j]; 
                    arr[j] = arr[j + 1]; 
                    arr[j + 1] = temp; 
                } 
            } 
        } 
 
        System.out.println(Arrays.toString(arr)); 
    } 
} 
``` 
[⬆ Back to Top](#top)
 
## Remove Duplicates from Array 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 2, 3, 4}; 
        Set<Integer> set = new LinkedHashSet<>(); 
 
        for (int num : arr) set.add(num); 
 
        System.out.println(set); 
    } 
} 
```
[⬆ Back to Top](#top)

## Count Words in String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "Hello Java World"; 
        String[] words = str.split(" "); 
        System.out.println(words.length); 
    } 
}
```
[⬆ Back to Top](#top)

## Find Second Largest Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {10, 20, 4, 45}; 
        int first = Integer.MIN_VALUE, second = Integer.MIN_VALUE; 
 
        for (int num : arr) { 
            if (num > first) { 
                second = first; 
                first = num; 
            } else if (num > second && num != first) { 
                second = num; 
            } 
        } 
 
        System.out.println(second); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Check Anagram 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        String s1 = "listen", s2 = "silent"; 
 
        char[] a = s1.toCharArray(); 
        char[] b = s2.toCharArray(); 
 
        Arrays.sort(a); 
        Arrays.sort(b); 
 
        System.out.println(Arrays.equals(a, b)); 
    } 
}
```
[⬆ Back to Top](#top)

## Matrix Addition 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] a = {{1,2},{3,4}}; 
        int[][] b = {{5,6},{7,8}}; 
        int[][] c = new int[2][2]; 
 
        for(int i=0;i<2;i++){ 
            for(int j=0;j<2;j++){ 
                c[i][j]=a[i][j]+b[i][j]; 
                System.out.print(c[i][j]+" "); 
            } 
            System.out.println(); 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Check Leap Year 
```java
public class Main { 
    public static void main(String[] args) { 
        int year = 2024; 
 
        if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) 
            System.out.println("Leap Year"); 
        else 
            System.out.println("Not Leap Year"); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Print Multiplication Table 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 5; 
 
        for (int i = 1; i <= 10; i++) { 
            System.out.println(n + " x " + i + " = " + (n * i)); 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Find Factor of Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 12; 
 
        for (int i = 1; i <= n; i++) { 
            if (n % i == 0) 
                System.out.print(i + " "); 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Sum of First N Natural Numbers 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 10, sum = 0; 
 
        for (int i = 1; i <= n; i++) { 
            sum += i; 
        } 
 
        System.out.println(sum); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Check Perfect Number 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 28, sum = 0; 
 
        for (int i = 1; i < n; i++) { 
            if (n % i == 0) 
                sum += i; 
        } 
 
        System.out.println(sum == n ? "Perfect Number" : "Not Perfect"); 
    } 
}
```
[⬆ Back to Top](#top)

## Count Frequency of Elements 
```java
import java.util.HashMap; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 2, 3, 3, 3}; 
 
        HashMap<Integer, Integer> map = new HashMap<>(); 
 
        for (int num : arr) { 
            map.put(num, map.getOrDefault(num, 0) + 1); 
        } 
 
        System.out.println(map); 
    } 
}
``` 
[⬆ Back to Top](#top)

## Find Missing Number in Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 4, 5}; 
        int n = 5; 
        int sum = n * (n + 1) / 2; 
 
        int arrSum = 0; 
        for (int num : arr) arrSum += num; 
 
        System.out.println(sum - arrSum); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Check Sorted Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4}; 
        boolean sorted = true; 
 
        for (int i = 1; i < arr.length; i++) { 
            if (arr[i] < arr[i - 1]) { 
                sorted = false; 
                break; 
            } 
        } 
 
        System.out.println(sorted); 
    } 
}
```
[⬆ Back to Top](#top)

## Rotate Array by One 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4}; 
 
        int last = arr[arr.length - 1]; 
 
        for (int i = arr.length - 1; i > 0; i--) { 
            arr[i] = arr[i - 1]; 
        } 
 
        arr[0] = last; 
 
        for (int num : arr) 
            System.out.print(num + " "); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Find Duplicate Element 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 2}; 
        HashSet<Integer> set = new HashSet<>(); 
 
        for (int num : arr) { 
            if (!set.add(num)) { 
                System.out.println("Duplicate: " + num); 
                break; 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Reverse Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3}; 
 
        for (int i = arr.length - 1; i >= 0; i--) { 
            System.out.print(arr[i] + " "); 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Merge Two Arrays 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1, 2}; 
        int[] b = {3, 4}; 
 
        int[] c = new int[a.length + b.length]; 
 
        System.arraycopy(a, 0, c, 0, a.length); 
        System.arraycopy(b, 0, c, a.length, b.length); 
 
        System.out.println(Arrays.toString(c)); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Find Common Elements 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1, 2, 3}; 
        int[] b = {2, 3, 4}; 
 
        HashSet<Integer> set = new HashSet<>(); 
 
        for (int num : a) set.add(num); 
 
        for (int num : b) { 
            if (set.contains(num)) 
                System.out.print(num + " "); 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Remove Spaces from String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "Hello World"; 
        str = str.replace(" ", ""); 
        System.out.println(str); 
    } 
} 
```
[⬆ Back to Top](#top)

## Convert String to Uppercase 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "java"; 
        System.out.println(str.toUpperCase()); 
    } 
}
```
[⬆ Back to Top](#top)

## Count Characters in String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "hello"; 
        System.out.println(str.length()); 
    } 
}
``` 
[⬆ Back to Top](#top)

## Find First Non-Repeating Character 
```java
import java.util.LinkedHashMap; 
 
public class Main { 
    public static void main(String[] args) { 
        String str = "swiss"; 
        LinkedHashMap<Character, Integer> map = new LinkedHashMap<>(); 
 
        for (char c : str.toCharArray()) { 
            map.put(c, map.getOrDefault(c, 0) + 1); 
        } 
 
        for (char c : map.keySet()) { 
            if (map.get(c) == 1) { 
                System.out.println(c); 
                break; 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)

## Check String Contains Only Digits 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "12345"; 
        boolean isDigit = str.matches("\\d+"); 
        System.out.println(isDigit); 
    } 
} 
```
[⬆ Back to Top](#top)

## Convert String to Integer 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "100"; 
        int num = Integer.parseInt(str); 
        System.out.println(num); 
    } 
} 
```
[⬆ Back to Top](#top)

## Find Largest Word in Sentence 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "Java is powerful language"; 
        String[] words = str.split(" "); 
 
        String largest = ""; 
        for (String word : words) { 
            if (word.length() > largest.length()) 
                largest = word; 
        } 
 
        System.out.println(largest); 
    } 
}
``` 
[⬆ Back to Top](#top)

## Check Two Strings Equal 
```java
public class Main { 
    public static void main(String[] args) { 
        String a = "java", b = "java"; 
        System.out.println(a.equals(b)); 
    } 
}
```
[⬆ Back to Top](#top)

## Toggle Case of Characters 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "Java"; 
 
        StringBuilder result = new StringBuilder(); 
 
        for (char c : str.toCharArray()) { 
            if (Character.isUpperCase(c)) 
                result.append(Character.toLowerCase(c)); 
            else 
                result.append(Character.toUpperCase(c)); 
        } 
 
        System.out.println(result); 
    } 
}
```
[⬆ Back to Top](#top)

## Count Occurrence of Character 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "banana"; 
        char ch = 'a'; 
        int count = 0; 
 
        for (char c : str.toCharArray()) { 
            if (c == ch) count++; 
        } 
 
        System.out.println(count); 
    } 
}
```
[⬆ Back to Top](#top)

## Remove Character from String 
```java
public class Main { 
    public static void main(String[] args) { 
        String str = "banana"; 
        str = str.replace("a", ""); 
        System.out.println(str); 
    } 
} 
```
[⬆ Back to Top](#top)

## Sort Characters in String 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        String str = "dcba"; 
 
        char[] arr = str.toCharArray(); 
        Arrays.sort(arr); 
 
        System.out.println(new String(arr)); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Sum of Even Numbers in Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4, 6}; 
        int sum = 0; 
 
        for (int num : arr) { 
            if (num % 2 == 0) sum += num; 
        } 
 
        System.out.println(sum); 
    } 
} 
```
[⬆ Back to Top](#top)

## Sum of Odd Numbers in Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4, 5}; 
        int sum = 0; 
 
        for (int num : arr) { 
            if (num % 2 != 0) sum += num; 
        } 
 
        System.out.println(sum); 
    } 
} 
```
[⬆ Back to Top](#top)

## Find Smallest Element 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {5, 2, 8, 1}; 
        int min = arr[0]; 
 
        for (int num : arr) { 
            if (num < min) min = num; 
        } 
 
        System.out.println(min); 
    } 
} 
``` 
[⬆ Back to Top](#top)

## Copy Array 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3}; 
        int[] copy = Arrays.copyOf(arr, arr.length); 
 
        System.out.println(Arrays.toString(copy)); 
    } 
} 
```
[⬆ Back to Top](#top)

## Check Array Equality 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1, 2, 3}; 
        int[] b = {1, 2, 3}; 
 
        System.out.println(Arrays.equals(a, b)); 
    } 
}
```
[⬆ Back to Top](#top)


## Find Index of Element 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {10, 20, 30}; 
        int key = 20; 
 
        for (int i = 0; i < arr.length; i++) { 
            if (arr[i] == key) { 
                System.out.println(i); 
                break; 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Count Positive and Negative Numbers 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {-1, 2, -3, 4}; 
        int pos = 0, neg = 0; 
 
        for (int num : arr) { 
            if (num >= 0) pos++; 
            else neg++; 
        } 
 
        System.out.println("Positive: " + pos + ", Negative: " + neg); 
    } 
} 
```
[⬆ Back to Top](#top)


## Move Zeros to End 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {0, 1, 0, 3, 12}; 
        int index = 0; 
 
        for (int num : arr) { 
            if (num != 0) arr[index++] = num; 
        } 
 
        while (index < arr.length) { 
            arr[index++] = 0; 
        } 
 
        for (int num : arr) 
            System.out.print(num + " "); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Find Intersection of Arrays 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1, 2, 3}; 
        int[] b = {2, 3, 4}; 
 
        HashSet<Integer> set = new HashSet<>(); 
        for (int num : a) set.add(num); 
 
        for (int num : b) { 
            if (set.contains(num)) 
                System.out.print(num + " "); 
        } 
    } 
} 
```
[⬆ Back to Top](#top)


## Find Union of Arrays 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1, 2}; 
        int[] b = {2, 3}; 
 
        HashSet<Integer> set = new HashSet<>(); 
 
        for (int num : a) set.add(num); 
        for (int num : b) set.add(num); 
 
        System.out.println(set); 
    } 
}
```
[⬆ Back to Top](#top)


## Reverse String Using Recursion 
```java
public class Main { 
    static void reverse(String str) { 
        if (str.isEmpty()) return; 
        reverse(str.substring(1)); 
        System.out.print(str.charAt(0)); 
    } 
 
    public static void main(String[] args) { 
        reverse("hello"); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Factorial Using Recursion 
```java
public class Main { 
    static int fact(int n) { 
        if (n == 0) return 1; 
        return n * fact(n - 1); 
    } 
 
    public static void main(String[] args) { 
        System.out.println(fact(5)); 
    } 
}
```
[⬆ Back to Top](#top)


## Fibonacci Using Recursion 
```java
public class Main { 
    static int fib(int n) { 
        if (n <= 1) return n; 
        return fib(n - 1) + fib(n - 2); 
    } 
 
    public static void main(String[] args) { 
        System.out.println(fib(6)); 
    } 
}
```
[⬆ Back to Top](#top)


## Power of Number (Recursion) 
```java
public class Main { 
    static int power(int a, int b) { 
        if (b == 0) return 1; 
        return a * power(a, b - 1); 
    } 
 
    public static void main(String[] args) { 
        System.out.println(power(2, 3)); 
    } 
} 
```
[⬆ Back to Top](#top)


## Sum of Digits (Recursion) 
```java
public class Main { 
    static int sum(int n) { 
        if (n == 0) return 0; 
        return n % 10 + sum(n / 10); 
    } 
 
    public static void main(String[] args) { 
        System.out.println(sum(123)); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Binary Search 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, 2, 3, 4, 5}; 
        int key = 4; 
        int low = 0, high = arr.length - 1; 
 
        while (low <= high) { 
            int mid = (low + high) / 2; 
 
            if (arr[mid] == key) { 
                System.out.println(mid); 
                return; 
            } else if (arr[mid] < key) { 
                low = mid + 1; 
            } else { 
                high = mid - 1; 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Selection Sort 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {4, 2, 7, 1}; 
 
        for (int i = 0; i < arr.length; i++) { 
            int min = i; 
 
            for (int j = i + 1; j < arr.length; j++) { 
                if (arr[j] < arr[min]) 
                    min = j; 
            } 
 
            int temp = arr[i]; 
            arr[i] = arr[min]; 
            arr[min] = temp; 
        } 
 
        System.out.println(Arrays.toString(arr)); 
    } 
} 
``` 
[⬆ Back to Top](#top)


## Insertion Sort 
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {5, 3, 1, 4}; 
 
        for (int i = 1; i < arr.length; i++) { 
            int key = arr[i]; 
            int j = i - 1; 
 
            while (j >= 0 && arr[j] > key) { 
                arr[j + 1] = arr[j]; 
                j--; 
            } 
 
            arr[j + 1] = key; 
        } 
 
        System.out.println(Arrays.toString(arr)); 
    } 
}
```
[⬆ Back to Top](#top)


## Check Prime (Optimized) 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 29; 
        boolean prime = true; 
 
        for (int i = 2; i <= Math.sqrt(n); i++) { 
            if (n % i == 0) { 
                prime = false; 
                break; 
            } 
        } 
 
        System.out.println(prime); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Find All Prime Numbers up to N 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 20; 
 
        for (int i = 2; i <= n; i++) { 
            boolean prime = true; 
 
            for (int j = 2; j <= Math.sqrt(i); j++) { 
                if (i % j == 0) { 
                    prime = false; 
                    break; 
                } 
            } 
 
            if (prime) System.out.print(i + " "); 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Transpose Matrix 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,2,3},{4,5,6}}; 
 
        for(int i=0;i<3;i++){ 
            for(int j=0;j<2;j++){ 
                System.out.print(mat[j][i]+" "); 
            } 
            System.out.println(); 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Diagonal Sum of Matrix 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,2,3},{4,5,6},{7,8,9}}; 
        int sum = 0; 
 
        for(int i=0;i<mat.length;i++){ 
            sum += mat[i][i]; 
        } 
 
        System.out.println(sum); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Check Identity Matrix 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,0,0},{0,1,0},{0,0,1}}; 
        boolean identity = true; 
 
        for(int i=0;i<mat.length;i++){ 
            for(int j=0;j<mat.length;j++){ 
                if((i==j && mat[i][j]!=1) || (i!=j && mat[i][j]!=0)){ 
                    identity = false; 
                } 
            } 
        } 
 
        System.out.println(identity); 
    } 
}
```
[⬆ Back to Top](#top)


## Check Sparse Matrix 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{0,0,3},{0,0,0},{1,0,0}}; 
        int zero = 0; 
 
        for(int[] row : mat){ 
            for(int val : row){ 
                if(val == 0) zero++; 
            } 
        } 
 
        if(zero > (mat.length * mat[0].length) / 2) 
            System.out.println("Sparse Matrix"); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Matrix Multiplication 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] a = {{1,2},{3,4}}; 
        int[][] b = {{5,6},{7,8}}; 
        int[][] c = new int[2][2]; 
 
        for(int i=0;i<2;i++){ 
            for(int j=0;j<2;j++){ 
                for(int k=0;k<2;k++){ 
                    c[i][j] += a[i][k]*b[k][j]; 
                } 
                System.out.print(c[i][j]+" "); 
            } 
            System.out.println(); 
        } 
    } 
} 
```
[⬆ Back to Top](#top)


## Two Sum Problem 
```java
import java.util.HashMap; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {2, 7, 11, 15}; 
        int target = 9; 
 
        HashMap<Integer, Integer> map = new HashMap<>(); 
 
        for (int i = 0; i < arr.length; i++) { 
            int diff = target - arr[i]; 
            if (map.containsKey(diff)) { 
                System.out.println(map.get(diff) + " " + i); 
                return; 
            } 
            map.put(arr[i], i); 
        } 
    } 
} 
```
[⬆ Back to Top](#top)


## Maximum Subarray Sum Kadanes Algorithm 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {-2,1,-3,4,-1,2,1,-5,4}; 
        int maxSum = arr[0], current = arr[0]; 
 
        for (int i = 1; i < arr.length; i++) { 
            current = Math.max(arr[i], current + arr[i]); 
            maxSum = Math.max(maxSum, current); 
        } 
 
        System.out.println(maxSum); 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Best Time to Buy and Sell Stock 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] prices = {7,1,5,3,6,4}; 
        int min = prices[0], profit = 0; 
 
        for (int price : prices) { 
            if (price < min) min = price; 
            else profit = Math.max(profit, price - min); 
        } 
 
        System.out.println(profit); 
    } 
} 
```
[⬆ Back to Top](#top)


## Move Negative Numbers to One Side 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1, -2, 3, -4, 5}; 
        int j = 0; 
 
        for (int i = 0; i < arr.length; i++) { 
            if (arr[i] < 0) { 
                int temp = arr[i]; 
                arr[i] = arr[j]; 
                arr[j] = temp; 
                j++; 
            } 
        } 
 
        for (int num : arr) 
            System.out.print(num + " "); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Longest Substring Without Repeating Characters 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        String s = "abcabcbb"; 
        HashSet<Character> set = new HashSet<>(); 
 
        int left = 0, max = 0; 
 
        for (int right = 0; right < s.length(); right++) { 
            while (set.contains(s.charAt(right))) { 
                set.remove(s.charAt(left++)); 
            } 
            set.add(s.charAt(right)); 
            max = Math.max(max, right - left + 1); 
        } 
 
        System.out.println(max); 
    } 
}
```
[⬆ Back to Top](#top)


## Subarray with Given Sum Positive Integers 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,2,3,7,5}; 
        int target = 12; 
 
        int start = 0, sum = 0; 
 
        for (int end = 0; end < arr.length; end++) { 
            sum += arr[end]; 
 
            while (sum > target) { 
                sum -= arr[start++]; 
            } 
 
            if (sum == target) { 
                System.out.println(start + " " + end); 
                return; 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Count Subarrays with Given Sum 
```java
import java.util.HashMap; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,1,1}; 
        int k = 2, count = 0, sum = 0; 
 
        HashMap<Integer, Integer> map = new HashMap<>(); 
        map.put(0, 1); 
 
        for (int num : arr) { 
            sum += num; 
 
            if (map.containsKey(sum - k)) 
                count += map.get(sum - k); 
 
            map.put(sum, map.getOrDefault(sum, 0) + 1); 
        } 
 
        System.out.println(count); 
    } 
} 
```
[⬆ Back to Top](#top)


## Majority Element Greater than n2 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {3,3,4}; 
        int count = 0, candidate = 0; 
 
        for (int num : arr) { 
            if (count == 0) candidate = num; 
            count += (num == candidate) ? 1 : -1; 
        } 
 
        System.out.println(candidate); 
    } 
} 
``` 
[⬆ Back to Top](#top)


## Missing Number 0 to n
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {3,0,1}; 
        int n = arr.length; 
        int sum = n * (n + 1) / 2; 
 
        int arrSum = 0; 
        for (int num : arr) arrSum += num; 
 
        System.out.println(sum - arrSum); 
    } 
}
```
[⬆ Back to Top](#top)


## Find Duplicate Number 
```java
import java.util.HashSet; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,3,4,2,2}; 
        HashSet<Integer> set = new HashSet<>(); 
 
        for (int num : arr) { 
            if (!set.add(num)) { 
                System.out.println(num); 
                break; 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Product of Array Except Self 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,2,3,4}; 
        int n = arr.length; 
 
        int[] res = new int[n]; 
        res[0] = 1; 
 
        for (int i = 1; i < n; i++) 
            res[i] = res[i - 1] * arr[i - 1]; 
 
        int right = 1; 
 
        for (int i = n - 1; i >= 0; i--) { 
            res[i] *= right; 
            right *= arr[i]; 
        } 
 
        for (int num : res) 
            System.out.print(num + " "); 
    } 
}
```
[⬆ Back to Top](#top)


## Maximum Product Subarray 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {2,3,-2,4}; 
        int max = arr[0], min = arr[0], result = arr[0]; 
 
        for (int i = 1; i < arr.length; i++) { 
            int temp = max; 
            max = Math.max(arr[i], Math.max(max * arr[i], min * arr[i])); 
            min = Math.min(arr[i], Math.min(temp * arr[i], min * arr[i])); 
            result = Math.max(result, max); 
        } 
 
        System.out.println(result); 
    } 
} 
``` 
 [⬆ Back to Top](#top)


## Rotate Array by K Steps 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,2,3,4,5}; 
        int k = 2; 
        int n = arr.length; 
 
        k = k % n; 
 
        reverse(arr, 0, n - 1); 
        reverse(arr, 0, k - 1); 
        reverse(arr, k, n - 1); 
 
        for (int num : arr) 
            System.out.print(num + " "); 
    } 
 
    static void reverse(int[] arr, int i, int j) { 
        while (i < j) { 
            int temp = arr[i]; 
            arr[i++] = arr[j]; 
            arr[j--] = temp; 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Trapping Rain Water 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] height = {0,1,0,2,1,0,1,3}; 
        int left = 0, right = height.length - 1; 
        int leftMax = 0, rightMax = 0, water = 0; 
 
        while (left < right) { 
            if (height[left] < height[right]) { 
                leftMax = Math.max(leftMax, height[left]); 
                water += leftMax - height[left]; 
                left++; 
            } else { 
                rightMax = Math.max(rightMax, height[right]); 
                water += rightMax - height[right]; 
                right--; 
            } 
        } 
 
        System.out.println(water); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Container With Most Water 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] h = {1,8,6,2,5,4,8,3,7}; 
        int left = 0, right = h.length - 1; 
        int max = 0; 
 
        while (left < right) { 
            int area = Math.min(h[left], h[right]) * (right - left); 
            max = Math.max(max, area); 
 
            if (h[left] < h[right]) left++; 
            else right--; 
        } 
 
        System.out.println(max); 
    } 
}
```
[⬆ Back to Top](#top)


## Merge Intervals 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int[][] intervals = {{1,3},{2,6},{8,10}}; 
        Arrays.sort(intervals, (a,b) -> a[0] - b[0]); 
 
        List<int[]> result = new ArrayList<>(); 
 
        for (int[] interval : intervals) { 
            if (result.isEmpty() || result.get(result.size()-1)[1] < interval[0]) { 
                result.add(interval); 
            } else { 
                result.get(result.size()-1)[1] = 
                        Math.max(result.get(result.size()-1)[1], interval[1]); 
            } 
        } 
 
        for (int[] i : result) 
            System.out.println(i[0] + " " + i[1]); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Set Matrix Zeroes 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,1,1},{1,0,1},{1,1,1}}; 
        boolean row0 = false, col0 = false; 
 
        for(int i=0;i<mat.length;i++) 
            if(mat[i][0]==0) col0=true; 
 
        for(int j=0;j<mat[0].length;j++) 
            if(mat[0][j]==0) row0=true; 
 
        for(int i=1;i<mat.length;i++){ 
            for(int j=1;j<mat[0].length;j++){ 
                if(mat[i][j]==0){ 
                    mat[i][0]=mat[0][j]=0; 
                } 
            } 
        } 
 
        for(int i=1;i<mat.length;i++){ 
            for(int j=1;j<mat[0].length;j++){ 
                if(mat[i][0]==0 || mat[0][j]==0) 
                    mat[i][j]=0; 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Spiral Matrix 
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,2,3},{4,5,6},{7,8,9}}; 
        int top=0, bottom=2, left=0, right=2; 
 
        while(top<=bottom && left<=right){ 
            for(int i=left;i<=right;i++) 
                System.out.print(mat[top][i]+" "); 
            top++; 
 
            for(int i=top;i<=bottom;i++) 
                System.out.print(mat[i][right]+" "); 
            right--; 
 
            for(int i=right;i>=left;i--) 
                System.out.print(mat[bottom][i]+" "); 
            bottom--; 
 
            for(int i=bottom;i>=top;i--) 
                System.out.print(mat[i][left]+" "); 
            left++; 
        } 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Search in Rotated Sorted Array 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {4,5,6,7,0,1,2}; 
        int target = 0; 
 
        int l = 0, r = arr.length - 1; 
 
        while (l <= r) { 
            int mid = (l + r) / 2; 
 
            if (arr[mid] == target) { 
                System.out.println(mid); 
                return; 
            } 
 
            if (arr[l] <= arr[mid]) { 
                if (target >= arr[l] && target < arr[mid]) 
                    r = mid - 1; 
                else l = mid + 1; 
            } else { 
                if (target > arr[mid] && target <= arr[r]) 
                    l = mid + 1; 
                else r = mid - 1; 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Find Peak Element 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,2,3,1}; 
        int l=0,r=arr.length-1; 
 
        while(l<r){ 
            int mid=(l+r)/2; 
            if(arr[mid]>arr[mid+1]) r=mid; 
            else l=mid+1; 
        } 
 
        System.out.println(l); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Kth Largest Element 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {3,2,1,5,6,4}; 
        int k = 2; 
 
        PriorityQueue<Integer> pq = new PriorityQueue<>(); 
 
        for(int num : arr){ 
            pq.add(num); 
            if(pq.size() > k) pq.poll(); 
        } 
 
        System.out.println(pq.peek()); 
    } 
}
```
[⬆ Back to Top](#top)


## Top K Frequent Elements 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {1,1,1,2,2,3}; 
        int k = 2; 
 
        HashMap<Integer,Integer> map = new HashMap<>(); 
        for(int num : arr) 
            map.put(num, map.getOrDefault(num,0)+1); 
 
        PriorityQueue<int[]> pq = new PriorityQueue<>((a,b)->a[1]-b[1]); 
 
        for(int key : map.keySet()){ 
            pq.add(new int[]{key,map.get(key)}); 
            if(pq.size()>k) pq.poll(); 
        } 
 
        while(!pq.isEmpty()) 
            System.out.print(pq.poll()[0]+" "); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Valid Parentheses 
```java
import java.util.Stack; 
 
public class Main { 
    public static void main(String[] args) { 
        String s = "()[]{}"; 
        Stack<Character> st = new Stack<>(); 
 
        for(char c : s.toCharArray()){ 
            if(c=='('||c=='['||c=='{') 
                st.push(c); 
            else{ 
                if(st.isEmpty()) return; 
                char top = st.pop(); 
                if((c==')'&&top!='(') || 
                   (c==']'&&top!='[') || 
                   (c=='}'&&top!='{')) 
                    return; 
            } 
        } 
        System.out.println(st.isEmpty()); 
    } 
}
```
[⬆ Back to Top](#top)


## Min Stack 
```java
import java.util.Stack; 
 
class MinStack { 
    Stack<Integer> stack = new Stack<>(); 
    Stack<Integer> min = new Stack<>(); 
 
    void push(int x){ 
        stack.push(x); 
        if(min.isEmpty() || x <= min.peek()) 
            min.push(x); 
    } 
 
    void pop(){ 
        if(stack.pop().equals(min.peek())) 
            min.pop(); 
    } 
 
    int getMin(){ 
        return min.peek(); 
    } 
} 
```  
[⬆ Back to Top](#top)

 
## Evaluate Postfix Expression 
```java
import java.util.Stack; 
 
public class Main { 
    public static void main(String[] args) { 
        String exp = "23*5+"; 
        Stack<Integer> st = new Stack<>(); 
 
        for(char c : exp.toCharArray()){ 
            if(Character.isDigit(c)) 
                st.push(c - '0'); 
            else{ 
                int b = st.pop(); 
                int a = st.pop(); 
 
                switch(c){ 
                    case '+': st.push(a+b); break; 
                    case '*': st.push(a*b); break; 
                } 
            } 
        } 
        System.out.println(st.pop()); 
    } 
}
```
[⬆ Back to Top](#top)


## Binary Tree Node Creation 
```java
class Node { 
    int data; 
    Node left, right; 
 
    Node(int val) { 
        data = val; 
        left = right = null; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Inorder Traversal Recursive
```java
class Node { 
    int data; 
    Node left, right; 
 
    Node(int val) { data = val; } 
} 
 
public class Main { 
    static void inorder(Node root) { 
        if (root == null) return; 
 
        inorder(root.left); 
        System.out.print(root.data + " "); 
        inorder(root.right); 
    } 
 
    public static void main(String[] args) { 
        Node root = new Node(1); 
        root.left = new Node(2); 
        root.right = new Node(3); 
 
        inorder(root); 
    } 
} 
```
[⬆ Back to Top](#top)


## Preorder Traversal 
```java
public class Main { 
    static void preorder(Node root) { 
        if (root == null) return; 
 
        System.out.print(root.data + " "); 
        preorder(root.left); 
        preorder(root.right); 
    } 
}
```
[⬆ Back to Top](#top)


## Postorder Traversal 
```java
public class Main { 
    static void postorder(Node root) { 
        if (root == null) return; 
 
        postorder(root.left); 
        postorder(root.right); 
        System.out.print(root.data + " "); 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Level Order Traversal BFS
```java
import java.util.*; 
 
public class Main { 
    static void levelOrder(Node root) { 
        Queue<Node> q = new LinkedList<>(); 
        q.add(root); 
 
        while (!q.isEmpty()) { 
            Node temp = q.poll(); 
            System.out.print(temp.data + " "); 
 
            if (temp.left != null) q.add(temp.left); 
            if (temp.right != null) q.add(temp.right); 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Height of Binary Tree 
```java
public class Main { 
    static int height(Node root) { 
        if (root == null) return 0; 
        return 1 + Math.max(height(root.left), height(root.right)); 
    } 
}
``` 
[⬆ Back to Top](#top)


## Count Nodes in Binary Tree 
```java
public class Main { 
    static int count(Node root) { 
        if (root == null) return 0; 
        return 1 + count(root.left) + count(root.right); 
    } 
} 
```
[⬆ Back to Top](#top)


## Sum of Nodes 
```java
public class Main { 
    static int sum(Node root) { 
        if (root == null) return 0; 
        return root.data + sum(root.left) + sum(root.right); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Check Balanced Binary Tree 
```java
public class Main { 
    static boolean isBalanced(Node root) { 
        if (root == null) return true; 
 
        int lh = height(root.left); 
        int rh = height(root.right); 
 
        return Math.abs(lh - rh) <= 1 && 
               isBalanced(root.left) && 
               isBalanced(root.right); 
    } 
}
```
[⬆ Back to Top](#top)


## Diameter of Binary Tree 
```java
public class Main { 
    static int diameter(Node root) { 
        if (root == null) return 0; 
 
        int lh = height(root.left); 
        int rh = height(root.right); 
 
        int ld = diameter(root.left); 
        int rd = diameter(root.right); 
 
        return Math.max(lh + rh + 1, Math.max(ld, rd)); 
    } 
}
```
[⬆ Back to Top](#top)


## Lowest Common Ancestor Binary Tree
```java
public class Main { 
    static Node lca(Node root, int n1, int n2) { 
        if (root == null || root.data == n1 || root.data == n2) 
            return root; 
 
        Node left = lca(root.left, n1, n2); 
        Node right = lca(root.right, n1, n2); 
 
        if (left != null && right != null) return root; 
        return (left != null) ? left : right; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Binary Search Tree Insert 
```java
public class Main { 
    static Node insert(Node root, int val) { 
        if (root == null) return new Node(val); 
 
        if (val < root.data) 
            root.left = insert(root.left, val); 
        else 
            root.right = insert(root.right, val); 
 
        return root; 
    } 
}
```
[⬆ Back to Top](#top)


## Search in BST 
```java
public class Main { 
    static boolean search(Node root, int key) { 
        if (root == null) return false; 
 
        if (root.data == key) return true; 
 
        if (key < root.data) 
            return search(root.left, key); 
        else 
            return search(root.right, key); 
    } 
} 
```  
[⬆ Back to Top](#top)

 
## Delete Node in BST 
```java
public class Main { 
    static Node delete(Node root, int key) { 
        if (root == null) return null; 
 
        if (key < root.data) 
            root.left = delete(root.left, key); 
        else if (key > root.data) 
            root.right = delete(root.right, key); 
        else { 
            if (root.left == null) return root.right; 
            if (root.right == null) return root.left; 
 
            Node min = findMin(root.right); 
            root.data = min.data; 
            root.right = delete(root.right, min.data); 
        } 
        return root; 
    } 
 
    static Node findMin(Node root) { 
        while (root.left != null) 
            root = root.left; 
        return root; 
    } 
}
```
[⬆ Back to Top](#top)


## Graph Representation Adjacency List
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int V = 5; 
        ArrayList<ArrayList<Integer>> graph = new ArrayList<>(); 
 
        for (int i = 0; i < V; i++) 
            graph.add(new ArrayList<>()); 
 
        graph.get(0).add(1); 
        graph.get(1).add(2); 
 
        System.out.println(graph); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## DFS Depth First Search
```java
import java.util.*; 
 
public class Main { 
    static void dfs(int node, boolean[] visited, ArrayList<ArrayList<Integer>> graph) { 
        visited[node] = true; 
        System.out.print(node + " "); 
 
        for (int neigh : graph.get(node)) { 
            if (!visited[neigh]) 
                dfs(neigh, visited, graph); 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## BFS (Graph) 
```java
import java.util.*; 
 
public class Main { 
    static void bfs(int start, ArrayList<ArrayList<Integer>> graph) { 
        boolean[] visited = new boolean[graph.size()]; 
        Queue<Integer> q = new LinkedList<>(); 
 
        q.add(start); 
        visited[start] = true; 
 
        while (!q.isEmpty()) { 
            int node = q.poll(); 
            System.out.print(node + " "); 
 
            for (int neigh : graph.get(node)) { 
                if (!visited[neigh]) { 
                    visited[neigh] = true; 
                    q.add(neigh); 
                } 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Detect Cycle in Undirected Graph 
```java
import java.util.*; 
 
public class Main { 
    static boolean dfs(int node, int parent, boolean[] visited, 
                       ArrayList<ArrayList<Integer>> graph) { 
 
        visited[node] = true; 
 
        for (int neigh : graph.get(node)) { 
            if (!visited[neigh]) { 
                if (dfs(neigh, node, visited, graph)) 
                    return true; 
            } else if (neigh != parent) { 
                return true; 
            } 
        } 
        return false; 
    } 
}
```
[⬆ Back to Top](#top)


## Number of Connected Components 
```java
public class Main { 
    static int countComponents(int V, ArrayList<ArrayList<Integer>> graph) { 
        boolean[] visited = new boolean[V]; 
        int count = 0; 
 
        for (int i = 0; i < V; i++) { 
            if (!visited[i]) { 
                dfs(i, visited, graph); 
                count++; 
            } 
        } 
        return count; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Flood Fill Algorithm 
```java
public class Main { 
    static void fill(int[][] image, int sr, int sc, int newColor, int oldColor) { 
        if (sr < 0 || sc < 0 || sr >= image.length || sc >= image[0].length) 
            return; 
 
        if (image[sr][sc] != oldColor) return; 
 
        image[sr][sc] = newColor; 
 
        fill(image, sr + 1, sc, newColor, oldColor); 
        fill(image, sr - 1, sc, newColor, oldColor); 
        fill(image, sr, sc + 1, newColor, oldColor); 
        fill(image, sr, sc - 1, newColor, oldColor); 
    } 
}
```
[⬆ Back to Top](#top)


## Number of Islands 
```java
public class Main { 
    static void dfs(char[][] grid, int i, int j) { 
        if (i < 0 || j < 0 || i >= grid.length || j >= grid[0].length 
            || grid[i][j] == '0') return; 
 
        grid[i][j] = '0'; 
 
        dfs(grid, i+1, j); 
        dfs(grid, i-1, j); 
        dfs(grid, i, j+1); 
        dfs(grid, i, j-1); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Topological Sort DFS
```java
import java.util.*; 
 
public class Main { 
    static void topo(int node, boolean[] visited, 
                     Stack<Integer> st, ArrayList<ArrayList<Integer>> graph) { 
 
        visited[node] = true; 
 
        for (int neigh : graph.get(node)) { 
            if (!visited[neigh]) 
                topo(neigh, visited, st, graph); 
        } 
 
        st.push(node); 
    } 
} 
```
[⬆ Back to Top](#top)


## Dijkstras Algorithm 
```java
import java.util.*; 
 
public class Main { 
    static void dijkstra(int src, ArrayList<ArrayList<int[]>> graph, int V) { 
        int[] dist = new int[V]; 
        Arrays.fill(dist, Integer.MAX_VALUE); 
 
        PriorityQueue<int[]> pq = new PriorityQueue<>((a,b)->a[1]-b[1]); 
 
        pq.add(new int[]{src, 0}); 
        dist[src] = 0; 
 
        while (!pq.isEmpty()) { 
            int[] cur = pq.poll(); 
            int node = cur[0], d = cur[1]; 
 
            for (int[] neigh : graph.get(node)) { 
                int next = neigh[0], weight = neigh[1]; 
 
                if (d + weight < dist[next]) { 
                    dist[next] = d + weight; 
                    pq.add(new int[]{next, dist[next]}); 
                } 
            } 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Bellman-Ford Algorithm 
```java
public class Main { 
    static void bellman(int V, int[][] edges, int src) { 
        int[] dist = new int[V]; 
        Arrays.fill(dist, Integer.MAX_VALUE); 
        dist[src] = 0; 
 
        for (int i = 0; i < V - 1; i++) { 
            for (int[] e : edges) { 
                int u = e[0], v = e[1], w = e[2]; 
                if (dist[u] != Integer.MAX_VALUE && 
                    dist[u] + w < dist[v]) { 
                    dist[v] = dist[u] + w; 
                } 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Disjoint Set Union-Find
```java
class DSU { 
    int[] parent; 
 
    DSU(int n) { 
        parent = new int[n]; 
        for (int i = 0; i < n; i++) 
            parent[i] = i; 
    } 
 
    int find(int x) { 
        if (parent[x] != x) 
            parent[x] = find(parent[x]); 
        return parent[x]; 
    } 
 
    void union(int a, int b) { 
        parent[find(a)] = find(b); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Fibonacci using DP Memoization
```java
import java.util.Arrays; 
 
public class Main { 
    static int fib(int n, int[] dp) { 
        if (n <= 1) return n; 
 
        if (dp[n] != -1) return dp[n]; 
 
        return dp[n] = fib(n - 1, dp) + fib(n - 2, dp); 
    } 
 
    public static void main(String[] args) { 
        int n = 6; 
        int[] dp = new int[n + 1]; 
        Arrays.fill(dp, -1); 
 
        System.out.println(fib(n, dp)); 
    } 
}
```
[⬆ Back to Top](#top)


## Fibonacci using Tabulation 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 6; 
        int[] dp = new int[n + 1]; 
 
        dp[0] = 0; 
        dp[1] = 1; 
 
        for (int i = 2; i <= n; i++) 
            dp[i] = dp[i - 1] + dp[i - 2]; 
 
        System.out.println(dp[n]); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Climbing Stairs 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 5; 
        int[] dp = new int[n + 1]; 
 
        dp[0] = 1; 
        dp[1] = 1; 
 
        for (int i = 2; i <= n; i++) 
            dp[i] = dp[i - 1] + dp[i - 2]; 
 
        System.out.println(dp[n]); 
    } 
}
```
[⬆ Back to Top](#top)


## House Robber Problem 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] nums = {2,7,9,3,1}; 
        int prev = 0, curr = 0; 
 
        for (int num : nums) { 
            int temp = Math.max(curr, prev + num); 
            prev = curr; 
            curr = temp; 
        } 
 
        System.out.println(curr); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Coin Change Minimum Coins
```java
import java.util.Arrays; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] coins = {1,2,5}; 
        int amount = 11; 
 
        int[] dp = new int[amount + 1]; 
        Arrays.fill(dp, amount + 1); 
        dp[0] = 0; 
 
        for (int coin : coins) { 
            for (int i = coin; i <= amount; i++) { 
                dp[i] = Math.min(dp[i], dp[i - coin] + 1); 
            } 
        } 
 
        System.out.println(dp[amount] > amount ? -1 : dp[amount]); 
    } 
}
```
[⬆ Back to Top](#top)


## Longest Increasing Subsequence 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] nums = {10,9,2,5,3,7,101,18}; 
        int n = nums.length; 
        int[] dp = new int[n]; 
 
        Arrays.fill(dp, 1); 
        int max = 1; 
 
        for (int i = 1; i < n; i++) { 
            for (int j = 0; j < i; j++) { 
                if (nums[i] > nums[j]) 
                    dp[i] = Math.max(dp[i], dp[j] + 1); 
            } 
            max = Math.max(max, dp[i]); 
        } 
 
        System.out.println(max); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Longest Common Subsequence 
```java
public class Main { 
    public static void main(String[] args) { 
        String a = "abcde", b = "ace"; 
        int m = a.length(), n = b.length(); 
 
        int[][] dp = new int[m + 1][n + 1]; 
 
        for (int i = 1; i <= m; i++) { 
            for (int j = 1; j <= n; j++) { 
                if (a.charAt(i - 1) == b.charAt(j - 1)) 
                    dp[i][j] = 1 + dp[i - 1][j - 1]; 
                else 
                    dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]); 
            } 
        } 
 
        System.out.println(dp[m][n]); 
    } 
}
```
[⬆ Back to Top](#top)


## Edit Distance 
```java
public class Main { 
    public static void main(String[] args) { 
        String a = "horse", b = "ros"; 
        int m = a.length(), n = b.length(); 
 
        int[][] dp = new int[m + 1][n + 1]; 
 
        for (int i = 0; i <= m; i++) dp[i][0] = i; 
        for (int j = 0; j <= n; j++) dp[0][j] = j; 
 
        for (int i = 1; i <= m; i++) { 
            for (int j = 1; j <= n; j++) { 
                if (a.charAt(i - 1) == b.charAt(j - 1)) 
                    dp[i][j] = dp[i - 1][j - 1]; 
                else 
                    dp[i][j] = 1 + Math.min(dp[i - 1][j], 
                               Math.min(dp[i][j - 1], dp[i - 1][j - 1])); 
            } 
        } 
 
        System.out.println(dp[m][n]); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## 0 1 Knapsack 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] wt = {1,3,4,5}; 
        int[] val = {1,4,5,7}; 
        int W = 7; 
 
        int[][] dp = new int[wt.length + 1][W + 1]; 
 
        for (int i = 1; i <= wt.length; i++) { 
            for (int w = 1; w <= W; w++) { 
                if (wt[i - 1] <= w) 
                    dp[i][w] = Math.max(val[i - 1] + dp[i - 1][w - wt[i - 1]], 
                                        dp[i - 1][w]); 
                else 
                    dp[i][w] = dp[i - 1][w]; 
            } 
        } 
 
        System.out.println(dp[wt.length][W]); 
    } 
}
```
[⬆ Back to Top](#top)


## Subset Sum Problem 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {2,3,7,8,10}; 
        int sum = 11; 
 
        boolean[][] dp = new boolean[arr.length + 1][sum + 1]; 
 
        for (int i = 0; i <= arr.length; i++) 
            dp[i][0] = true; 
 
        for (int i = 1; i <= arr.length; i++) { 
            for (int j = 1; j <= sum; j++) { 
                if (arr[i - 1] <= j) 
                    dp[i][j] = dp[i - 1][j] || dp[i - 1][j - arr[i - 1]]; 
                else 
                    dp[i][j] = dp[i - 1][j]; 
            } 
        } 
 
        System.out.println(dp[arr.length][sum]); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Partition Equal Subset Sum 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] nums = {1,5,11,5}; 
        int sum = 0; 
 
        for (int num : nums) sum += num; 
 
        if (sum % 2 != 0) { 
            System.out.println(false); 
            return; 
        } 
 
        int target = sum / 2; 
        boolean[] dp = new boolean[target + 1]; 
        dp[0] = true; 
 
        for (int num : nums) { 
            for (int j = target; j >= num; j--) { 
                dp[j] = dp[j] || dp[j - num]; 
            } 
        } 
 
        System.out.println(dp[target]); 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Minimum Path Sum Grid
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] grid = {{1,3,1},{1,5,1},{4,2,1}}; 
        int m = grid.length, n = grid[0].length; 
 
        int[][] dp = new int[m][n]; 
 
        dp[0][0] = grid[0][0]; 
 
        for (int i = 1; i < m; i++) 
            dp[i][0] = dp[i - 1][0] + grid[i][0]; 
 
        for (int j = 1; j < n; j++) 
            dp[0][j] = dp[0][j - 1] + grid[0][j]; 
 
        for (int i = 1; i < m; i++) { 
            for (int j = 1; j < n; j++) { 
                dp[i][j] = grid[i][j] + 
                           Math.min(dp[i - 1][j], dp[i][j - 1]); 
            } 
        } 
 
        System.out.println(dp[m - 1][n - 1]); 
    } 
}
```
[⬆ Back to Top](#top)


## Unique Paths 
```java
public class Main { 
    public static void main(String[] args) { 
        int m = 3, n = 3; 
        int[][] dp = new int[m][n]; 
 
        for (int i = 0; i < m; i++) dp[i][0] = 1; 
        for (int j = 0; j < n; j++) dp[0][j] = 1; 
 
        for (int i = 1; i < m; i++) { 
            for (int j = 1; j < n; j++) { 
                dp[i][j] = dp[i - 1][j] + dp[i][j - 1]; 
            } 
        } 
 
        System.out.println(dp[m - 1][n - 1]); 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Palindromic Substrings Count 
```java
public class Main { 
    public static void main(String[] args) { 
        String s = "aaa"; 
        int count = 0; 
 
        for (int center = 0; center < s.length(); center++) { 
            int l = center, r = center; 
            while (l >= 0 && r < s.length() && 
                   s.charAt(l) == s.charAt(r)) { 
                count++; 
                l--; r++; 
            } 
 
            l = center; r = center + 1; 
            while (l >= 0 && r < s.length() && 
                   s.charAt(l) == s.charAt(r)) { 
                count++; 
                l--; r++; 
            } 
        } 
 
        System.out.println(count); 
    } 
}
``` 
[⬆ Back to Top](#top)

 
## Longest Palindromic Substring 
```java
public class Main { 
    public static void main(String[] args) { 
        String s = "babad"; 
        String res = ""; 
 
        for (int i = 0; i < s.length(); i++) { 
            String p1 = expand(s, i, i); 
            String p2 = expand(s, i, i + 1); 
 
            if (p1.length() > res.length()) res = p1; 
            if (p2.length() > res.length()) res = p2; 
        } 
 
        System.out.println(res); 
    } 
 
    static String expand(String s, int l, int r) { 
        while (l >= 0 && r < s.length() && 
               s.charAt(l) == s.charAt(r)) { 
            l--; r++; 
        } 
        return s.substring(l + 1, r); 
    } 
}
```
[⬆ Back to Top](#top)


## Decode Ways 
```java
public class Main { 
    public static void main(String[] args) { 
        String s = "226"; 
        int n = s.length(); 
 
        int[] dp = new int[n + 1]; 
        dp[0] = 1; 
        dp[1] = s.charAt(0) == '0' ? 0 : 1; 
 
        for (int i = 2; i <= n; i++) { 
            int one = Integer.parseInt(s.substring(i - 1, i)); 
            int two = Integer.parseInt(s.substring(i - 2, i)); 
 
            if (one >= 1) dp[i] += dp[i - 1]; 
            if (two >= 10 && two <= 26) 
                dp[i] += dp[i - 2]; 
        } 
 
        System.out.println(dp[n]); 
    } 
} 
```
[⬆ Back to Top](#top)

  
## Word Break 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        String s = "leetcode"; 
        Set<String> dict = new HashSet<>(Arrays.asList("leet","code")); 
 
        boolean[] dp = new boolean[s.length() + 1]; 
        dp[0] = true; 
 
        for (int i = 1; i <= s.length(); i++) { 
            for (int j = 0; j < i; j++) { 
                if (dp[j] && dict.contains(s.substring(j, i))) { 
                    dp[i] = true; 
                    break; 
                } 
            } 
        } 
 
        System.out.println(dp[s.length()]); 
    } 
}
```
[⬆ Back to Top](#top)


## Longest Common Substring 
```java
public class Main { 
    public static void main(String[] args) { 
        String a = "abc", b = "bcd"; 
        int max = 0; 
 
        int[][] dp = new int[a.length()+1][b.length()+1]; 
 
        for(int i=1;i<=a.length();i++){ 
            for(int j=1;j<=b.length();j++){ 
                if(a.charAt(i-1)==b.charAt(j-1)){ 
                    dp[i][j]=1+dp[i-1][j-1]; 
                    max=Math.max(max,dp[i][j]); 
                } 
            } 
        } 
 
        System.out.println(max); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Rod Cutting Problem 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] price = {2,5,7,8}; 
        int n = 4; 
 
        int[] dp = new int[n + 1]; 
 
        for (int i = 1; i <= n; i++) { 
            for (int j = 0; j < i; j++) { 
                dp[i] = Math.max(dp[i], price[j] + dp[i - j - 1]); 
            } 
        } 
 
        System.out.println(dp[n]); 
    } 
}
```
[⬆ Back to Top](#top)


## Matrix Chain Multiplication 
```java
public class Main { 
    static int solve(int[] arr, int i, int j) { 
        if (i == j) return 0; 
 
        int min = Integer.MAX_VALUE; 
 
        for (int k = i; k < j; k++) { 
            int cost = solve(arr, i, k) + 
                       solve(arr, k + 1, j) + 
                       arr[i - 1] * arr[k] * arr[j]; 
 
            min = Math.min(min, cost); 
        } 
        return min; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Egg Dropping Problem 
```java
public class Main { 
    static int eggDrop(int e, int f) { 
        if (f == 0 || f == 1) return f; 
        if (e == 1) return f; 
 
        int min = Integer.MAX_VALUE; 
 
        for (int k = 1; k <= f; k++) { 
            int temp = 1 + Math.max( 
                eggDrop(e - 1, k - 1), 
                eggDrop(e, f - k) 
            ); 
            min = Math.min(min, temp); 
        } 
        return min; 
    } 
}
```
[⬆ Back to Top](#top)


## Wildcard Matching 
```java
public class Main { 
    public static void main(String[] args) { 
        String s = "aa", p = "*"; 
        boolean[][] dp = new boolean[s.length()+1][p.length()+1]; 
 
        dp[0][0] = true; 
 
        for(int j=1;j<=p.length();j++){ 
            if(p.charAt(j-1)=='*') 
                dp[0][j]=dp[0][j-1]; 
        } 
 
        for(int i=1;i<=s.length();i++){ 
            for(int j=1;j<=p.length();j++){ 
                if(p.charAt(j-1)=='*') 
                    dp[i][j]=dp[i][j-1]||dp[i-1][j]; 
                else if(p.charAt(j-1)=='?' || s.charAt(i-1)==p.charAt(j-1)) 
                    dp[i][j]=dp[i-1][j-1]; 
            } 
        } 
 
        System.out.println(dp[s.length()][p.length()]); 
    } 
}
``` 
[⬆ Back to Top](#top)

 
## Burst Balloons 
```java
public class Main { 
    static int solve(int[] nums, int l, int r) { 
        if (l > r) return 0; 
 
        int max = 0; 
 
        for (int i = l; i <= r; i++) { 
            int coins = nums[l - 1] * nums[i] * nums[r + 1] + 
                        solve(nums, l, i - 1) + 
                        solve(nums, i + 1, r); 
 
            max = Math.max(max, coins); 
        } 
        return max; 
    } 
}
```
[⬆ Back to Top](#top)


## Palindrome Partitioning 
```java
public class Main { 
    static int minCuts(String s, int i, int j) { 
        if (i >= j || isPal(s, i, j)) return 0; 
 
        int min = Integer.MAX_VALUE; 
 
        for (int k = i; k < j; k++) { 
            int temp = 1 + minCuts(s, i, k) + minCuts(s, k + 1, j); 
            min = Math.min(min, temp); 
        } 
        return min; 
    } 
 
    static boolean isPal(String s, int i, int j) { 
        while (i < j) { 
            if (s.charAt(i++) != s.charAt(j--)) 
                return false; 
        } 
        return true; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Boolean Parenthesization 
```java
public class Main { 
    static int solve(String s, int i, int j, boolean isTrue) { 
        if (i > j) return 0; 
        if (i == j) { 
            if (isTrue) return s.charAt(i) == 'T' ? 1 : 0; 
            else return s.charAt(i) == 'F' ? 1 : 0; 
        } 
 
        int ways = 0; 
 
        for (int k = i + 1; k < j; k += 2) { 
            int lt = solve(s, i, k - 1, true); 
            int lf = solve(s, i, k - 1, false); 
            int rt = solve(s, k + 1, j, true); 
            int rf = solve(s, k + 1, j, false); 
 
            char op = s.charAt(k); 
 
            if (op == '&') { 
                if (isTrue) ways += lt * rt; 
                else ways += lt * rf + lf * rt + lf * rf; 
            } 
        } 
        return ways; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Sliding Window Maximum 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] nums = {1,3,-1,-3,5,3,6,7}; 
        int k = 3; 
 
        Deque<Integer> dq = new LinkedList<>(); 
 
        for (int i = 0; i < nums.length; i++) { 
            if (!dq.isEmpty() && dq.peek() == i - k) 
                dq.poll(); 
 
            while (!dq.isEmpty() && nums[dq.peekLast()] < nums[i]) 
                dq.pollLast(); 
 
            dq.add(i); 
 
            if (i >= k - 1) 
                System.out.print(nums[dq.peek()] + " "); 
        } 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Minimum Window Substring 
```java
import java.util.*; 
 
public class Main { 
    public static void main(String[] args) { 
        String s = "ADOBECODEBANC", t = "ABC"; 
        int[] map = new int[128]; 
 
        for (char c : t.toCharArray()) map[c]++; 
 
        int left = 0, right = 0, count = t.length(); 
        int minLen = Integer.MAX_VALUE, start = 0; 
 
        while (right < s.length()) { 
            if (map[s.charAt(right++)]-- > 0) count--; 
 
            while (count == 0) { 
                if (right - left < minLen) { 
                    minLen = right - left; 
                    start = left; 
                } 
 
                if (map[s.charAt(left++)]++ == 0) count++; 
            } 
        } 
 
        System.out.println(minLen == Integer.MAX_VALUE ? "" : 
                s.substring(start, start + minLen)); 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Largest Rectangle in Histogram 
```java
import java.util.Stack; 
 
public class Main { 
    public static void main(String[] args) { 
        int[] heights = {2,1,5,6,2,3}; 
        Stack<Integer> st = new Stack<>(); 
        int max = 0; 
 
        for (int i = 0; i <= heights.length; i++) { 
            int h = (i == heights.length) ? 0 : heights[i]; 
 
            while (!st.isEmpty() && h < heights[st.peek()]) { 
                int height = heights[st.pop()]; 
                int width = st.isEmpty() ? i : i - st.peek() - 1; 
                max = Math.max(max, height * width); 
            } 
            st.push(i); 
        } 
 
        System.out.println(max); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Maximal Rectangle Binary Matrix
```java
public class Main { 
    public static void main(String[] args) { 
        int[][] mat = {{1,0,1,0},{1,0,1,1},{1,1,1,1}}; 
        int[] heights = new int[mat[0].length]; 
        int max = 0; 
 
        for (int[] row : mat) { 
            for (int j = 0; j < row.length; j++) { 
                heights[j] = (row[j] == 0) ? 0 : heights[j] + 1; 
            } 
            max = Math.max(max, largest(heights)); 
        } 
 
        System.out.println(max); 
    } 
 
    static int largest(int[] h) { 
        Stack<Integer> st = new Stack<>(); 
        int max = 0; 
 
        for (int i = 0; i <= h.length; i++) { 
            int height = (i == h.length) ? 0 : h[i]; 
 
            while (!st.isEmpty() && height < h[st.peek()]) { 
                int ht = h[st.pop()]; 
                int w = st.isEmpty() ? i : i - st.peek() - 1; 
                max = Math.max(max, ht * w); 
            } 
            st.push(i); 
        } 
        return max; 
    } 
} 
```
[⬆ Back to Top](#top)

 
## Median of Two Sorted Arrays 
```java
public class Main { 
    public static void main(String[] args) { 
        int[] a = {1,3}; 
        int[] b = {2}; 
 
        int[] merged = new int[a.length + b.length]; 
        int i=0,j=0,k=0; 
 
        while(i<a.length && j<b.length){ 
            if(a[i]<b[j]) merged[k++]=a[i++]; 
            else merged[k++]=b[j++]; 
        } 
 
        while(i<a.length) merged[k++]=a[i++]; 
        while(j<b.length) merged[k++]=b[j++]; 
 
        int n = merged.length; 
 
        if(n%2==0) 
            System.out.println((merged[n/2-1]+merged[n/2])/2.0); 
        else 
            System.out.println(merged[n/2]); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Merge K Sorted Lists 
```java
import java.util.*; 
 
class ListNode { 
    int val; 
    ListNode next; 
    ListNode(int x) { val = x; } 
} 
 
public class Main { 
    static ListNode merge(ListNode[] lists) { 
        PriorityQueue<ListNode> pq = 
            new PriorityQueue<>((a,b)->a.val-b.val); 
 
        for(ListNode node : lists) 
            if(node!=null) pq.add(node); 
 
        ListNode dummy = new ListNode(0), curr = dummy; 
 
        while(!pq.isEmpty()){ 
            ListNode node = pq.poll(); 
            curr.next = node; 
            curr = curr.next; 
 
            if(node.next!=null) pq.add(node.next); 
        } 
        return dummy.next; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## LRU Cache 
```java
import java.util.*; 
 
class LRUCache { 
    int capacity; 
    LinkedHashMap<Integer,Integer> map; 
 
    LRUCache(int cap){ 
        capacity = cap; 
        map = new LinkedHashMap<>(cap,0.75f,true){ 
            protected boolean removeEldestEntry(Map.Entry e){ 
                return size()>capacity; 
            } 
        }; 
    } 
 
    int get(int key){ 
        return map.getOrDefault(key,-1); 
    } 
 
    void put(int key,int value){ 
        map.put(key,value); 
    } 
}
```
[⬆ Back to Top](#top)


## LFU Cache Basic
```java
import java.util.*; 
 
class LFUCache { 
    HashMap<Integer,Integer> map = new HashMap<>(); 
 
    void put(int key,int val){ 
        map.put(key,val); 
    } 
 
    int get(int key){ 
        return map.getOrDefault(key,-1); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Serialize and Deserialize Binary Tree 
```java
import java.util.*; 
 
public class Main { 
    static String serialize(Node root){ 
        if(root==null) return "null"; 
        return root.data + "," + 
               serialize(root.left) + "," + 
               serialize(root.right); 
    } 
} 
```
[⬆ Back to Top](#top)


## Trie Implementation 
```java
class Trie { 
    Trie[] children = new Trie[26]; 
    boolean isEnd; 
 
    void insert(String word){ 
        Trie node = this; 
        for(char c:word.toCharArray()){ 
            int i=c-'a'; 
            if(node.children[i]==null) 
                node.children[i]=new Trie(); 
            node=node.children[i]; 
        } 
        node.isEnd=true; 
    } 
}
```
[⬆ Back to Top](#top)


## Word Search Backtracking
```java
public class Main { 
    static boolean exist(char[][] board,String word,int i,int j,int k){ 
        if(k==word.length()) return true; 
        if(i<0||j<0||i>=board.length||j>=board[0].length|| 
           board[i][j]!=word.charAt(k)) return false; 
 
        char temp=board[i][j]; 
        board[i][j]='#'; 
 
        boolean found = exist(board,word,i+1,j,k+1) || 
                        exist(board,word,i-1,j,k+1) || 
                        exist(board,word,i,j+1,k+1) || 
                        exist(board,word,i,j-1,k+1); 
 
        board[i][j]=temp; 
        return found; 
    } 
}
``` 
[⬆ Back to Top](#top)

 
## N-Queens Problem 
```java
public class Main { 
    static boolean isSafe(char[][] board,int r,int c){ 
        for(int i=0;i<r;i++) 
            if(board[i][c]=='Q') return false; 
 
        for(int i=r-1,j=c-1;i>=0&&j>=0;i--,j--) 
            if(board[i][j]=='Q') return false; 
 
        for(int i=r-1,j=c+1;i>=0&&j<board.length;i--,j++) 
            if(board[i][j]=='Q') return false; 
 
        return true; 
    } 
}
```
[⬆ Back to Top](#top)


## Sudoku Solver 
```java
public class Main { 
    static boolean solve(char[][] board){ 
        for(int i=0;i<9;i++){ 
            for(int j=0;j<9;j++){ 
                if(board[i][j]=='.'){ 
                    for(char c='1';c<='9';c++){ 
                        if(valid(board,i,j,c)){ 
                            board[i][j]=c; 
                            if(solve(board)) return true; 
                            board[i][j]='.'; 
                        } 
                    } 
                    return false; 
                } 
            } 
        } 
        return true; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## KMP String Matching 
```java
public class Main { 
    static void lps(String pat,int[] lps){ 
        int len=0,i=1; 
 
        while(i<pat.length()){ 
            if(pat.charAt(i)==pat.charAt(len)) 
                lps[i++]=++len; 
            else if(len!=0) 
                len=lps[len-1]; 
            else lps[i++]=0; 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Rabin-Karp Algorithm 
```java
public class Main { 
    static void search(String txt,String pat){ 
        int m=pat.length(),n=txt.length(); 
 
        for(int i=0;i<=n-m;i++){ 
            if(txt.substring(i,i+m).equals(pat)) 
                System.out.println(i); 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Z Algorithm 
```java
public class Main { 
    static void zAlgo(String s){ 
        int n=s.length(); 
        int[] z=new int[n]; 
 
        int l=0,r=0; 
 
        for(int i=1;i<n;i++){ 
            if(i<=r) 
                z[i]=Math.min(r-i+1,z[i-l]); 
 
            while(i+z[i]<n && s.charAt(z[i])==s.charAt(i+z[i])) 
                z[i]++; 
 
            if(i+z[i]-1>r){ 
                l=i; 
                r=i+z[i]-1; 
            } 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Quick Sort 
```java
public class Main { 
    static void quickSort(int[] arr,int low,int high){ 
        if(low<high){ 
            int pi=partition(arr,low,high); 
            quickSort(arr,low,pi-1); 
            quickSort(arr,pi+1,high); 
        } 
    } 
 
    static int partition(int[] arr,int low,int high){ 
        int pivot=arr[high],i=low-1; 
 
        for(int j=low;j<high;j++){ 
            if(arr[j]<pivot){ 
                i++; 
                int t=arr[i];arr[i]=arr[j];arr[j]=t; 
            } 
        } 
        int t=arr[i+1];arr[i+1]=arr[high];arr[high]=t; 
        return i+1; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Merge Sort 
```java
public class Main { 
    static void mergeSort(int[] arr,int l,int r){ 
        if(l<r){ 
            int m=(l+r)/2; 
            mergeSort(arr,l,m); 
            mergeSort(arr,m+1,r); 
            merge(arr,l,m,r); 
        } 
    } 
 
    static void merge(int[] arr,int l,int m,int r){ 
        int[] temp=new int[r-l+1]; 
        int i=l,j=m+1,k=0; 
 
        while(i<=m && j<=r) 
            temp[k++]=arr[i]<arr[j]?arr[i++]:arr[j++]; 
 
        while(i<=m) temp[k++]=arr[i++]; 
        while(j<=r) temp[k++]=arr[j++]; 
 
        for(int x=0;x<temp.length;x++) 
            arr[l+x]=temp[x]; 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Heap Sort 
```java
public class Main { 
    static void heapSort(int[] arr){ 
        int n=arr.length; 
 
        for(int i=n/2-1;i>=0;i--) 
            heapify(arr,n,i); 
 
        for(int i=n-1;i>=0;i--){ 
            int t=arr[0];arr[0]=arr[i];arr[i]=t; 
            heapify(arr,i,0); 
        } 
    } 
 
    static void heapify(int[] arr,int n,int i){ 
        int largest=i,l=2*i+1,r=2*i+2; 
 
        if(l<n && arr[l]>arr[largest]) largest=l; 
        if(r<n && arr[r]>arr[largest]) largest=r; 
 
        if(largest!=i){ 
            int t=arr[i];arr[i]=arr[largest];arr[largest]=t; 
            heapify(arr,n,largest); 
        } 
    } 
}
```
[⬆ Back to Top](#top)


## Counting Sort 
```java
public class Main { 
    static void countSort(int[] arr){ 
        int max=Arrays.stream(arr).max().getAsInt(); 
        int[] count=new int[max+1]; 
 
        for(int num:arr) count[num]++; 
 
        int k=0; 
        for(int i=0;i<count.length;i++){ 
            while(count[i]-- >0) 
                arr[k++]=i; 
        } 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Radix Sort 
```java
public class Main { 
    static void radixSort(int[] arr){ 
        int max=Arrays.stream(arr).max().getAsInt(); 
 
        for(int exp=1;max/exp>0;exp*=10) 
            countSort(arr,exp); 
    } 
}
```
[⬆ Back to Top](#top)


## Bucket Sort 
```java
import java.util.*; 
 
public class Main { 
    static void bucketSort(float[] arr){ 
        int n=arr.length; 
 
        List<Float>[] buckets=new ArrayList[n]; 
 
        for(int i=0;i<n;i++) 
            buckets[i]=new ArrayList<>(); 
 
        for(float num:arr) 
            buckets[(int)(num*n)].add(num); 
 
        for(List<Float> b:buckets) 
            Collections.sort(b); 
 
        int k=0; 
        for(List<Float> b:buckets) 
            for(float num:b) 
                arr[k++]=num; 
    } 
}
```
[⬆ Back to Top](#top)


## Bit Manipulation Count Set Bits
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 5, count = 0; 
 
        while (n > 0) { 
            count += n & 1; 
            n >>= 1; 
        } 
 
        System.out.println(count); 
    } 
} 
``` 
[⬆ Back to Top](#top)

 
## Power of Two Check 
```java
public class Main { 
    public static void main(String[] args) { 
        int n = 8; 
        System.out.println((n & (n - 1)) == 0); 
    } 
}
```
[⬆ Back to Top](#top)


## Single Number XOR
```java
public class Main { 
    public static void main(String[] args) { 
        int[] arr = {2,2,1}; 
 
        int res = 0; 
        for (int num : arr) 
            res ^= num; 
 
        System.out.println(res); 
    } 
}
```
[⬆ Back to Top](#top)

