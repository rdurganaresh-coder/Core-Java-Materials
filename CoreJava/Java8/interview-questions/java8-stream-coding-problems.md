# 500 Java 8 Stream Coding Problems

A comprehensive practice set of **Java 8 Stream coding problems** used in backend developer interviews.

---

# Section 1 — Basic Stream Operations (1–50)

1. Convert a List to Stream and print elements.
2. Filter even numbers from a list.
3. Filter odd numbers from a list.
4. Convert list of strings to uppercase.
5. Convert list of strings to lowercase.
6. Remove duplicate elements from a list.
7. Sort integers in ascending order.
8. Sort integers in descending order.
9. Find maximum number in a list.
10. Find minimum number in a list.
11. Count total elements in a list.
12. Sum all numbers in a list.
13. Find average of numbers.
14. Filter numbers greater than 50.
15. Filter numbers less than 10.
16. Print first 5 numbers from a list.
17. Skip first 5 numbers and print remaining.
18. Remove null values from list.
19. Count number of even numbers.
20. Count number of odd numbers.
21. Multiply all numbers in a list.
22. Print unique elements from list.
23. Sort strings alphabetically.
24. Reverse sort strings.
25. Find length of each string in list.
26. Filter strings starting with "A".
27. Filter strings ending with "Z".
28. Find strings containing substring "Java".
29. Find longest string in list.
30. Find shortest string in list.
31. Find second largest number.
32. Find second smallest number.
33. Find top 3 largest numbers.
34. Find bottom 3 smallest numbers.
35. Convert array to stream.
36. Convert stream to array.
37. Print squares of numbers.
38. Print cubes of numbers.
39. Filter positive numbers.
40. Filter negative numbers.
41. Find numbers divisible by 3.
42. Find numbers divisible by 5.
43. Find numbers divisible by both 3 and 5.
44. Count strings with length > 5.
45. Count strings with length < 3.
46. Print distinct squares of numbers.
47. Find first element in list.
48. Find any element in list.
49. Check if all numbers are positive.
50. Check if any number is negative.

---

# Section 2 — Intermediate Stream Problems (51–120)

51. Chain multiple filters.
52. Filter and map integers to strings.
53. Convert integers to their squares then sort.
54. Filter employees with salary > 50000.
55. Filter employees with age > 30.
56. Map employee names to uppercase.
57. Extract employee names list.
58. Extract employee salaries list.
59. Extract employee IDs.
60. Sort employees by name.
61. Sort employees by salary.
62. Sort employees by age.
63. Sort employees by department.
64. Find employee with max salary.
65. Find employee with min salary.
66. Find employee with longest name.
67. Find employee with shortest name.
68. Get distinct departments.
69. Count employees per department.
70. Find employees in IT department.
71. Filter employees joined after 2020.
72. Filter employees with experience > 5 years.
73. Convert employee list to map by ID.
74. Convert employee list to map by name.
75. Group employees by department.
76. Group employees by age.
77. Partition employees by salary > 50k.
78. Partition numbers into even and odd.
79. Find duplicate numbers in list.
80. Find duplicate strings in list.
81. Remove duplicate employees.
82. Find common elements between two lists.
83. Find union of two lists.
84. Find difference between two lists.
85. Find intersection between lists.
86. Flatten list of lists.
87. Flatten list of arrays.
88. Flatten nested objects.
89. Convert nested lists to single list.
90. Create infinite stream using iterate.
91. Create infinite stream using generate.
92. Generate first 10 Fibonacci numbers.
93. Generate random numbers using stream.
94. Limit infinite stream.
95. Skip elements in infinite stream.
96. Find first repeating character.
97. Find first non-repeating character.
98. Count frequency of characters.
99. Count frequency of words.
100. Reverse each word using streams.
101. Reverse full string using streams.
102. Find palindrome strings in list.
103. Count palindromes.
104. Remove palindromes.
105. Find longest palindrome.
106. Find shortest palindrome.
107. Convert list of objects to map.
108. Merge two maps using streams.
109. Sort map by key.
110. Sort map by value.
111. Reverse map entries.
112. Find top 5 highest values in map.
113. Filter map values > 100.
114. Convert map to list.
115. Convert map to set.
116. Extract keys from map.
117. Extract values from map.
118. Convert map entries to stream.
119. Sum map values.
120. Find max value in map.

---

# Section 3 — Real Interview Coding Problems (121–250)

121. Find second highest salary.
122. Find third highest salary.
123. Find employees earning above average salary.
124. Find department with highest salary.
125. Find department with lowest salary.
126. Calculate total salary per department.
127. Calculate average salary per department.
128. Find employee with second highest salary.
129. Find employee with lowest salary.
130. Find youngest employee.
131. Find oldest employee.
132. Find employees hired this year.
133. Find employees hired last year.
134. Count employees hired per year.
135. Find employees with duplicate names.
136. Remove employees with duplicate IDs.
137. Find most frequent number in list.
138. Find least frequent number.
139. Find median value.
140. Find mode value.
141. Find product of numbers.
142. Calculate factorial using streams.
143. Generate prime numbers using streams.
144. Filter prime numbers from list.
145. Count prime numbers.
146. Find largest prime number.
147. Find smallest prime number.
148. Check if list contains only primes.
149. Generate squares of first 100 numbers.
150. Generate cubes of first 50 numbers.
151. Sum squares of numbers.
152. Sum cubes of numbers.
153. Find numbers whose square > 100.
154. Find numbers whose cube > 1000.
155. Find longest word in sentence.
156. Find shortest word in sentence.
157. Count words in sentence.
158. Count characters in sentence.
159. Find duplicate words in sentence.
160. Find most frequent word.
161. Find least frequent word.
162. Remove stop words from sentence.
163. Convert sentence to title case.
164. Sort words alphabetically.
165. Sort words by length.
166. Sort words by frequency.
167. Reverse words order.
168. Reverse characters of each word.
169. Find words starting with vowel.
170. Find words ending with consonant.
171. Count vowels in sentence.
172. Count consonants in sentence.
173. Find longest vowel sequence.
174. Find longest consonant sequence.
175. Generate random passwords using streams.
176. Generate random OTP.
177. Generate random UUID list.
178. Filter valid email addresses.
179. Filter valid phone numbers.
180. Remove invalid records.
181. Process log files using streams.
182. Find most frequent log entry.
183. Find error logs.
184. Find warning logs.
185. Find logs by timestamp.
186. Count requests per endpoint.
187. Count errors per service.
188. Find top requested API.
189. Find slowest API calls.
190. Find fastest API calls.
191. Calculate average response time.
192. Calculate max response time.
193. Calculate min response time.
194. Filter requests > 1s.
195. Filter requests < 100ms.
196. Find peak traffic hour.
197. Find least traffic hour.
198. Group logs by service.
199. Group logs by status code.
200. Find top 10 users by requests.
     201–250. Additional variations of employee, log processing, and collection manipulation problems using Streams.

---

# Section 4 — Advanced Stream Challenges (251–400)

251. Implement custom collector.
252. Build collector for average calculation.
253. Build collector for median.
254. Build collector for top N values.
255. Create immutable collector.
256. Implement groupingByConcurrent example.
257. Implement parallel stream processing.
258. Compare loop vs stream performance.
259. Process large files using streams.
260. Process large CSV dataset.
261. Implement sliding window using streams.
262. Implement batching logic.
263. Implement stream retry logic.
264. Handle exceptions inside stream pipeline.
265. Log intermediate stream operations.
266. Debug complex stream pipeline.
267. Build reusable stream utility functions.
268. Implement map-reduce pattern.
269. Implement associative reduction.
270. Build custom Spliterator.
271. Implement parallel file processing.
272. Stream database query results.
273. Stream JSON records.
274. Stream XML records.
275. Process event streams.
276. Implement streaming aggregation.
277. Implement streaming analytics.
278. Create stream pipeline for ETL.
279. Optimize nested streams.
280. Optimize parallel streams.
     281–400. Advanced variations combining grouping, mapping, partitioning, flatMap, reduce, and custom collectors.

---

# Section 5 — Expert-Level Stream Challenges (401–500)

401. Build real-time analytics pipeline.
402. Process millions of records using streams.
403. Implement reactive-like stream processing.
404. Build custom stream operator.
405. Implement windowed aggregation.
406. Implement event stream filtering.
407. Implement deduplication pipeline.
408. Implement distributed stream logic.
409. Implement streaming joins.
410. Build streaming recommendation engine.
411. Implement fraud detection pipeline.
412. Build log monitoring pipeline.
413. Process Kafka-style event streams.
414. Implement dynamic grouping logic.
415. Implement dynamic aggregation logic.
416. Optimize memory usage in streams.
417. Implement parallel batch processing.
418. Implement streaming ETL transformation.
419. Process IoT sensor streams.
420. Build monitoring dashboard pipeline.
     421–500. Complex real-world distributed stream processing challenges.

---

# Section 6:

## Core Concepts:
1. Why are Java Streams lazy, and how does laziness affect performance?

2.  Why can a Stream be consumed only once?

3. Difference between intermediate vs terminal operations with examples.

4. What makes an operation stateful vs stateless?

5. Why are stateful operations problematic in parallel streams?

6. Explain internal iteration vs external iteration.

7. How does short-circuiting work in streams?

8. What is the role of Spliterator in streams?

9. How does Java decide parallel splitting strategy?

10. Why is forEach() different from forEachOrdered()?
## Functional Operations

Difference between map() and flatMap() with real use cases.

When would you use reduce() vs collect()?

Why is reduce() dangerous with mutable objects?

What are identity, accumulator, combiner in reduce?

Explain associativity requirement in parallel streams.

How does peek() behave and why is it discouraged?

Difference between limit() and skip() in ordered vs unordered streams.

What is the cost of distinct() and sorted()?

How does filter() interact with short-circuiting operations?

What happens when you chain multiple intermediate operations?

## Parallel Streams (Critical)

How does parallelStream() work internally?

What thread pool is used by parallel streams?

Why are parallel streams dangerous in web applications?

What are race conditions in streams?

Why is shared mutable state unsafe in parallel streams?

What happens if you use ArrayList in parallel forEach()?

When do parallel streams perform worse than sequential?

What is the cost of maintaining order in parallel streams?

How do you make a stream operation thread-safe?

Why should blocking I/O not be used in parallel streams?

## Collectors & Customization

How does Collectors.groupingBy() work internally?

Difference between groupingBy() and partitioningBy().

What is a downstream collector?

How does collectingAndThen() work?

When would you write a custom collector?

What are the components of a custom collector?

What is the role of combiner() in parallel streams?

What happens if combiner is incorrect?

Explain collector characteristics (CONCURRENT, UNORDERED, etc.).

When is it safe to use CONCURRENT collectors?

## Coding & Transformation

Flatten nested collections using streams.

Group objects by multiple fields.

Find top K elements using streams.

Implement distinct by custom key.

Find duplicates in a stream.

Find first non-repeating element.

Convert list to map with duplicate keys handling.

Merge two streams efficiently.

Sort map by values using streams.

Perform multi-level grouping with aggregation.

## Edge Cases & Pitfalls

What happens if you reuse a stream?

Why can streams cause memory issues?

What is boxing/unboxing overhead in streams?

Why is Stream.generate() dangerous?

What happens with infinite streams?

Why is peek() not reliable for debugging?

What are side effects in streams?

Why are streams not suitable for stateful logic?

What happens if exceptions occur inside streams?

Why is debugging streams harder than loops?

## System Design + Streams

Why are Java Streams not suitable for distributed systems?

What is missing compared to Kafka/Flink streams?

Why is backpressure not supported?

How would you implement batch processing using streams?

Why are streams not ideal for real-time pipelines?

How do streams behave with large datasets?

What are limitations of streams in I/O-heavy tasks?

How do you handle retries in stream pipelines?

Why are streams unsuitable for event-time processing?

What are alternatives to streams in distributed environments?

## Bar-Raiser / Trick Questions

Can streams be made stateful safely?

Why does reduce() require associativity?

What breaks if associativity is violated?

Can you control thread pool used by parallel streams?

Why does forEach() behave unpredictably in parallel?

What happens if collector is not thread-safe?

Why is limit() expensive in parallel streams?

Can streams guarantee ordering and performance together?

Why are streams bad for LRU cache implementation?

What happens if you modify source collection during stream?

# End

Practice these **500 problems** to master **Java 8 Streams for real-world backend interviews**.
